# Build Flow 需求文档

## 概述
Build Flow 是产线规划中的建筑产线可视化区域，用于展示产线组的物料需求和绑定关系。

## 核心需求

### 1. 渲染显示
- 页面刷新后，建筑产线区域必须正确显示已有内容
- 容器 DOM 必须始终存在（使用 `v-show` 而非 `v-if`），确保 X6 Graph 实例能正确初始化
- 画布尺寸应自动适配内容，不能无限扩大

### 2. 组（Group）行为
- 组是**纯视觉背景容器**，仅用于视觉区分不同产线组
- 组**不可拖拽**，仅作为卡片和标签的视觉背景
- 组节点应始终位于卡片节点下方（z-index 层级）

### 3. 卡片（Card）行为
- 卡片（产线卡、产出卡）**可在组内自由拖拽移动位置**
- 卡片拖拽不应带动组移动
- 卡片内的标签区域不应触发卡片拖拽

### 4. 标签（Tag）拖拽绑定
- 卡片内的 **source tag**（产线产出物料）支持 HTML5 拖拽
- 拖拽 source tag 到 **target tag**（产线原材料/产出）可建立连线绑定
- 连线必须**从 tag 本身出发到对应的 tag**，而不是从组或卡片节点出发
- 标签区域的事件应隔离：
  - 标签上的 HTML5 拖拽（`dragstart/dragover/drop`）正常工作
  - 标签上的点击（`click`）正常工作
  - 标签区域不触发 X6 节点拖拽

### 5. 连线（Edge）显示
- 新建的连线必须**实时显示**在画布上
- 连线起点和终点应精确对应 tag 的位置
- 连线使用 SVG `<path>` 渲染，带箭头标识方向

### 6. 组布局规则
- **1 个组**：占满画布宽度
- **2 个组**：各占一半宽度（减去 gap），并排显示
- **3+ 个组**：每行最多 2 个，第 3 个开始换行显示
- 组之间保持固定 gap 间距

### 7. 事件隔离
- X6 节点拖拽（卡片移动）和 HTML5 拖拽（标签绑定）必须正确隔离
- 卡片空白区域可拖拽移动卡片
- 标签区域拖拽触发绑定，不触发卡片移动
- 使用 CSS `pointer-events` 或事件拦截实现分层

## 技术约束
- 使用 AntV X6 v3 作为图形库
- Vue 3 组件作为 X6 自定义节点（`@antv/x6-vue-shape`）
- HTML5 Drag & Drop API 用于标签绑定
- Tailwind CSS 用于样式

## 已知问题
1. `v-if="hasContent"` 导致初始不渲染时容器不存在，后续 graph 无法创建
2. X6 Vue 节点渲染在 `<foreignObject>` 的 DOM 层，与 SVG 事件系统冲突
3. 标签拖拽事件被 X6 节点拖拽拦截
4. 连线从组节点出发而非从 tag 出发
5. 新建连线未实时同步到画布
