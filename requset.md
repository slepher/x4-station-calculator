# 需求记录（暂存）

## 1. 空间站页面菜单栏调整
- 将菜单栏中的“导入”按钮移动到最右侧，并右对齐。
- 原“空间站名称设置栏”需要扩展，占用“原名称栏 + 原导入按钮”的空间。

## 2. 帝国总览页面新增导入按钮
- 在帝国总览菜单栏新增“导入”按钮。
- 按钮样式与空间站页面一致。
- 按钮放在菜单栏最右侧并右对齐。

## 3. 导入入口与导入界面
- 空间站页面与帝国总览页面点击“导入”后，都打开导入界面。
- 导入界面的风格和交互参考现有“载入界面”。
- 帝国总览支持导入逻辑组网的某个方案。
- 空间站页面支持导入逻辑组网某方案下的某个规划区。

## 4. 导入前确认流程
### 4.1 帝国总览导入
- 若当前帝国已存在空间站，则弹出 `SmartSaveDialog`。
- 选项为：
  - 保存并导入
  - 放弃并导入
- “保存并导入”沿用现有保存机制（同一存档体系）。

### 4.2 空间站页面导入
- 直接询问：
  - 导入为新空间站
  - 覆盖当前空间站
- 选择“覆盖当前空间站”时：先清空当前空间站，再执行导入。
- 选择“导入为新空间站”时：导入完成后自动切换到新空间站。

## 5. 数据导入映射规则
### 5.1 整个组网方案导入（帝国总览）
- 为组网方案中的每个规划区创建一个空间站。
- 对每个规划区，将其中每个 `manual` 节点作为一个模块插入目标站的 `plannedModules`。

### 5.2 单个规划区导入（空间站页面）
- 对该规划区执行同样映射：每个 `manual` 节点作为一个模块插入 `plannedModules`。

## 6. isolated 资源锁定规则
- 仅锁定“被导入规划区”里声明的 `isolated` 资源列表。
- 使用空间站现有的 `lockWares` 功能执行锁定。
