# 需求说明

为飞船蓝图载入功能添加默认预设：

- 载入界面原先显示所有保存的飞船蓝图，现在额外提供四个默认蓝图。
- 这四个默认蓝图不可以删除，也不是真的存在 storage 里面。
- 四个默认蓝图名称：空配、低配、中配、高配。

## 规则

- 空配：只有飞船，没有装备。
- 低配 / 中配 / 高配：
  - 先按 race 匹配装备；匹配不上则放松 race 显示。
  - 低配优先匹配 mk1。
  - 中配优先匹配 mk2，没有则下降到 mk1。
  - 高配优先匹配 mk 最高的装备。
  - 如果候选装备有多个，直接取第一个。

## 引擎与推进器优先级

- 对于 `purposePrimary: "fight"`：
  - 优先匹配战斗引擎。
  - 其次全功能引擎。
  - 其次巡航引擎。
- 对于非 `fight`：
  - 上述优先级反过来。

## 采矿船炮塔规则

- 对于 `purposePrimary: mine` 的飞船炮塔：
  - 先匹配 `slotTags` 包含 `mine` 的炮塔。

## U 槽无人机规则

- 对于 L 以及以上级别飞船，且 `purposePrimary: mine`：
  - U 槽提供 1 个运输无人机，9 个采矿无人机。
- 对于运输船，`purposePrimary: "trade"`：
  - U 槽全部使用运输无人机。
- 对于建造船，`purposePrimary: "trade"`：
  - U 槽全部使用建造无人机。
