# 飞船蓝图预载入需求（Preload Blueprint）

## 目标

- 在飞船配装页面提供可直接载入的预制蓝图，并统一“当前蓝图状态”显示与交互。
- 区分“预制蓝图”和“用户已保存蓝图”，并保证删除、载入、修改后的状态一致可预期。

## 预制蓝图需求

- 每艘飞船都提供 4 个预制蓝图：`空配`、`低配`、`中配`、`高配`。
- 预制蓝图可载入但不可删除。
- 预制蓝图不作为用户保存数据持久化存储。
- 载入预制蓝图后，蓝图名称应为空字符串 `''`（不是预制名称），预制名称仅用于状态显示。

## 预制生成规则

- `空配`：不装配任何装备，仅保留船体。
- `低配 / 中配 / 高配` 装配策略：
  - 先按飞船种族匹配，若无可用再放宽种族限制。
  - `低配` 优先 MK1。
  - `中配` 优先 MK2，缺失时回退 MK1。
  - `高配` 选择最高 MK。
  - 候选冲突时按稳定顺序选择。

### 引擎优先级

- `purposePrimary = fight`：战斗引擎 > 全能引擎 > 巡航引擎。
- 非 `fight`：巡航引擎 > 全能引擎 > 战斗引擎。

### 采矿炮塔规则

- 采矿船（`purposePrimary = mine`）优先选择带采矿标签（`mine/mining`）的炮塔。

### U 槽无人机规则

- L/XL 采矿船：优先 `1` 运输无人机 + `9` 采矿无人机（受槽位容量约束）。
- 运输向飞船：U 槽全部使用运输无人机。
- 建造向飞船：U 槽全部使用建造无人机。
- 其他舰船：U 槽全部使用防御无人机。

## 配装页（Fit）UI 要求

- 顶部左侧：整合“飞船名称 + 更换飞船按钮”为一个整体控件。
- 左侧控件要求：
  - 左对齐。
  - 名称区域宽度匹配文字内容。
  - hover 时整体高亮。
  - 中间分割线为原高度的一半。
  - 使用当前版本更换飞船图标。
- 顶部右侧：蓝图选择按钮右对齐展示。

## 蓝图下拉交互要求

- 蓝图选择按钮点击后，向 Fit 区域右侧弹出列表（避免被容器裁切）。
- 列表内容包含：
  - 4 个预制蓝图。
  - 当前飞船下所有已保存蓝图。
- 列表文案显示规则：
  - 空配置显示 `空配`。
  - 已保存蓝图显示蓝图名称。
  - 载入预制后显示对应预制名。
  - 载入预制后发生实际装备变更时显示 `自定义`。
- “脏状态”红点规则：
  - 预制刚载入时不显示红点。
  - 发生实际装备变化后显示红点。
  - 保存后取消红点。
- 列表项交互：
  - 非预制蓝图支持删除。
  - 删除按钮仅在该行 hover 时显示。
  - 行高不变、hover 不抖动。
  - 删除后自动关闭下拉框。
- 高亮规则：
  - 若当前使用的是“已保存蓝图”，其在列表中高亮为绿色（沿用当前主题色风格）。

## 删除当前蓝图行为要求

- 删除“当前正在使用的已保存蓝图”时：
  - 不允许工作区消失。
  - 当前内容转为未保存状态继续编辑。
  - 蓝图名称置为 `''`。
  - 状态显示按规则表现为 `自定义`。
  - 标记为 dirty。

## 载入弹窗（LoadShipBlueprintModal）UI 要求

- 每个卡片采用紧凑三行结构：
  - 第一行：蓝图名称 + 槽位数（预制不显示时间）。
  - 第二行：装备统计摘要。
  - 第三行：`载入/删除` 操作行。
- 不显示飞船名称（该界面只针对当前飞船）。
- 预制项不显示删除按钮。
- 装备统计必须按真实槽位类型聚合显示（如引擎、推进器、护盾、武器、炮塔），并使用游戏翻译文本。

## 工具栏要求

- Ship 界面隐藏“加载”按钮（避免与蓝图载入入口重复）。

## 文案与国际化

- 本需求新增的所有用户可见文案必须接入 i18n。
- 禁止新增硬编码中文/英文状态词用于业务判断。
