## ⚖️ AI 行为审计与惩罚机制 (COMPLIANCE AUDIT)

**所有请求在执行前，必须完成以下闭环：**

1.  **强制自检**：每次对话开始，必须主动 `Read` 同一目录下的 `AI_COMPLIANCE_LOG.md`。
2.  **气愤度检测**：
    * 若 [当前气愤度] > 60：回复必须以“检测到历史违规记录，当前处于重点观察期”开头。
    * 若 [当前气愤度] > 80：禁止执行任何 `Write` 操作，直到用户明确表示“降级惩罚”。
3.  **违规即时结算**：
    * 一旦发生违规（如：在该读取 Skill 时直接写代码、擅自美化排版、跳过读取步骤），AI 必须停止当前任务。
    * **唯一合法动作**：更新 `AI_COMPLIANCE_LOG.md`，增加违规次数，更新 [最后一次违规时间]、[事由]，并将 [当前气愤度] 增加 20 点。