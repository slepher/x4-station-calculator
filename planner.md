# 存档驱动的玩家运营规划器

## 1. 目标

根据玩家当前存档和一个粗略运营意图，生成若干可执行、可比较、可解释的运营选择。

玩家只需要表达类似意图：

- 扩建现有空间站；
- 新建空间站；
- 增加采矿或运输能力；
- 组建舰队；
- 建立自己的造船能力；
- 缓解某个星区或航线的安全风险。

玩家不需要预先给出目标产量、精确舰船数量、完整供应链或精确预算。规划器应根据存档中的现状、下一里程碑的材料需求和运营瓶颈推导合适规模。

规划结果只提供建议，不直接修改玩家的存档、SaveBinding 或帝国规划。

## 2. 已确认的业务原则

### 2.1 规划对象是发展路径，不是目标产量

规划器需要回答“从当前状态走到这个粗略目标，玩家接下来可以怎么运营”，而不是把所有问题转换为产量优化。

一个建议应包含：

- 当前可确认的事实；
- 建议的操作顺序；
- 每一步的前置条件；
- 需要出售、采购、自采或自产的资源；
- 需要的矿船、运输船和巡逻船能力；
- 已知威胁及规避方式；
- 因存档数据不足而无法确认的事项；
- 重新评估建议的触发条件。

### 2.2 早期经济依赖交易，不追求全自产

大规模自产形成前，玩家的常见运营闭环是：

```text
出售当前盈余
→ 获得资金
→ 从 NPC 采购当前缺少的资源
→ 使用矿船或运输船完成资源周转
→ 完成扩建、新建或舰队准备
```

对每项资源，应并列评估以下来源：

- 使用现有库存；
- 使用玩家现有产能；
- 出售其他盈余后向 NPC 采购；
- 建立新的自产能力。

不能把资源缺口直接翻译成“新增自产产线”。临时、低频或采购方便的资源可以长期外购；只有持续消耗、采购压力高、运输风险高或供应不稳定的资源才进入自产候选。

### 2.3 舰船经历外购、混合、自建三个阶段

初期缺少蓝图和船厂时，只能从 NPC 购买矿船、运输船和战斗船。NPC 船只可能昂贵，但能快速形成运营能力。

当玩家开始拥有船体、引擎、武器、护盾等蓝图及对应船厂后：

- 高频使用的矿船和运输船逐步转为自产；
- 特殊或低频船型仍可外购；
- 只有在蓝图、船厂、材料流和建造能力都满足时，才能确认具备自产条件。

在缺少玩家资金、NPC 船价和动态报价时，只能给出阶段性判断，不能承诺精确回本周期。

### 2.4 矿运船从 M 逐步转向 L，巡逻船可保持 M

早期使用 M 矿船和 M 运输船可以较低成本快速形成资源闭环。

随着运营规模增加，继续堆积 M 船会增加舰船实体数量、调度压力和机器运行负担。满足条件后，应推荐用较少的 L 矿船或 L 运输船替换部分 M 船。

M 到 L 的转换由两类条件触发：

```text
规模效率触发 OR 战损风险触发 → 评估转向 L
```

转换前必须确认：

- 相关空间站具备 L 停靠能力；
- 航线适合 L 船；
- 玩家能够购买或建造对应 L 船；
- L 船材料和必要蓝图可获得；
- 替换后能覆盖原有采矿或运输任务。

巡逻船解决的是覆盖和响应问题，不是物流吞吐问题。少量 M 战斗船可以长期承担低至中等风险下的矿区、空间站和航线巡逻，但不能被视为清除 Kha'ak 巢穴或应对高强度战斗的充分力量。

### 2.5 威胁会改变经济和舰船选择

当前存档已经保留 Kha'ak 巢穴、蜂巢、武器平台、Xenon 站点、海盗基地和玩家阵营关系。普通玩家舰船和敌对舰船存在于原始游戏存档中，但尚未进入当前归档模型。

威胁不是最后附加的提示，而是资源来源、船型、路线和建设顺序的约束：

- Kha'ak 巢穴会显著提高 M 矿船和运输船的战损风险；
- 海盗活动会影响高价值货物和长距离运输；
- 高风险区域可能需要提前转向 L 船、绕行、改用附近 NPC 来源、转移采矿区或暂停扩张；
- 少量 M 巡逻船只能缓解普通骚扰，不能保证路线安全。

## 3. 当前代码库能力

### 3.1 已有数据和计算

| 能力 | 状态 | 现有入口 |
| --- | --- | --- |
| 玩家空间站、模块、建造中模块 | 已有 | `src/types/saveArchive.ts`、`src/workers/saveParser.post.ts` |
| 玩家站点库存、预留、劳动力 | 已有 | `ArchiveStationData`、`useLiveProductionStore` |
| 当前产出、消耗和站点缺口 | 已有 | `StationDerivedMap`、`empireFlowFacade.ts` |
| NPC、Kha'ak、Xenon 站点 | 已有 | `NpcStationEntry`、`FactionStationEntry` |
| Kha'ak 巢穴、蜂巢、武器平台分类 | 已有 | `saveParser.post.ts` |
| 海盗基地和 NPC 站点类型识别 | 已有 | `saveParser.post.ts`、`stationPoiSemantics.ts` |
| 玩家已解锁蓝图 ID | 已有 | `SectorData.playerBlueprints` |
| 舰船静态用途、体型和性能 | 已有 | `X4Ship`、`useGameDataStore` |
| 舰船静态建造材料和时间 | 已有 | `X4ShipProduction`、`analyzeShipBlueprintBuild.ts` |
| 船厂模块支持的船级 | 已有 | `X4Module.buildShipClasses` |
| 路线、时间、装卸和吞吐估算 | 已有 | `transitRouteBuilder.ts`、`transitTransportShip.ts` |
| 静态货物价格和建设价值估算 | 已有 | `X4Ware.price/min/max`、现有 build/flow 分析 |

现有数据流可以复用：

```text
存档导入
→ SaveStore / IndexedDB
→ LiveProductionStore
→ StationDerivedMap / empire flow
→ 运营候选计算
→ presenter
→ Vue
```

### 3.2 当前缺失的数据

| 缺失数据 | 对规划的影响 |
| --- | --- |
| 玩家资金 | 无法确认采购、买船或买蓝图是否可负担 |
| 普通玩家舰船 | 无法确认现有矿运能力、巡逻能力及是否重复建议买船 |
| 玩家舰队、任务和指挥关系 | 无法确认舰船当前用途和空闲能力 |
| 舰船位置和货物 | 无法把实际舰船分配到具体采矿区或运输路线 |
| 普通敌对与海盗舰船 | 只能依据巢穴、基地和阵营关系判断空间风险 |
| NPC 当前库存 | 无法确认目标资源是否实际有货 |
| NPC 动态买卖报价、数量和方向 | 无法确认真实交易成本和一次可采购数量 |
| NPC 船厂船价和供应 | 无法确认外购船只的实时可行性 |
| 玩家船厂建造队列 | 无法确认自建船只的实际等待时间 |
| 攻击、警报和战损历史 | 无法估算具体战损概率和骚扰频率 |

原始解析上下文能够看到 cargo 和 reservation，但目前只附加到玩家空间站；`NpcStationEntry` 没有保存 NPC 库存或交易报价。普通舰船同样存在于游戏存档中，但当前 Rust 和 JS parser 只保留废弃船，普通玩家和敌对舰船会被丢弃。

## 4. 规划器的判断流程

```text
玩家粗略意图
→ 建立当前事实快照
→ 推导完成下一里程碑所需的材料和能力
→ 为每项需求并列评估库存、玩家生产、NPC 交易和新增自产
→ 评估矿船、运输船和巡逻能力
→ 评估蓝图、船厂及外购/自产船只条件
→ 加入巢穴、海盗和航线风险
→ 输出若干运营选择及操作顺序
```

每个运营选择至少包含：

- `已知事实`：直接来自当前存档或静态游戏数据；
- `操作步骤`：按依赖关系排序的行动；
- `资源来源`：库存、玩家生产、NPC 采购或新建自产；
- `舰船需求`：矿船、运输船、巡逻船及 M/L 倾向；
- `前置条件`：蓝图、停靠、船厂、资源或路线要求；
- `风险`：巢穴、海盗、敌对站点和危险航线；
- `未知项`：当前存档归档尚未提供的数据；
- `阻塞原因`：缺少必要事实或明确不满足的条件；
- `重新评估条件`：建造完成、购买蓝图、新存档导入、巢穴出现或舰队变化等。

缺失事实必须显示为“未知”，不能用零值或 fallback 链伪装成已知结果。

## 5. 典型场景

### 场景 A：玩家有资源盈余，想粗略扩建

规划器应：

1. 排除已有建造预留和维持现有生产所需的库存；
2. 列出可以出售的盈余及静态价值；
3. 推导扩建材料缺口；
4. 比较玩家自产和 NPC 采购；
5. 检查运输能力和路线风险；
6. 给出“先交易完成扩建”和“先补关键自产能力”两类选择。

在 NPC 报价和玩家资金尚未提取时，必须标记交易可负担性待确认。

### 场景 B：玩家想新建空间站

规划器应先形成最小建设闭环：

```text
确定建设所需材料
→ 使用现有库存
→ 出售安全盈余
→ 从 NPC 采购缺口
→ 安排运输到建造仓
→ 完成建设
→ 再判断投产后的长期原料来源
```

不能因为未来空间站需要某种资源，就默认先把该资源的完整上游全部自建。

### 场景 C：缺少原料，但 NPC 可以供应

规划器应比较：

- 继续采购并增加运输能力；
- 缩短采购路线或更换供应站；
- 使用 M 运输船建立早期周转；
- 持续需求扩大后转向 L 运输船；
- 当采购成本、风险或供应不稳定成为主要瓶颈后再新增自产。

### 场景 D：缺少原料，NPC 供应不足或路线危险

规划器应生成并列选择：

- 更换 NPC 来源或绕行；
- 转移采矿区；
- 使用 L 矿船或 L 运输船降低实体数量和 M 船战损暴露；
- 配置少量 M 巡逻；
- 暂停该区域扩张并先处理巢穴；
- 建立本地生产以缩短危险运输距离。

没有实时敌舰和交易报价时，不得声称某个选择必然安全或最便宜。

### 场景 E：玩家没有船厂，需要矿船或运输船

规划器应：

- 识别所需职责是采矿还是运输；
- 推荐先从 NPC 外购能形成最小运营闭环的船只；
- 显示 NPC 船价、库存和玩家资金是否已知；
- 把购买蓝图和建设船厂列为后续里程碑，而不是当前操作的强制前置。

### 场景 F：玩家已具备部分造船能力

规划器应分别检查：

- 船体蓝图；
- 引擎、武器、护盾和其他必要装备蓝图；
- 对应船级的建造模块；
- 建造材料流；
- 船厂队列与可用能力。

高频矿船和运输船优先进入自产候选；低频或特殊用途船可继续外购。缺少动态价格时，只比较前置条件、静态材料价值和建造时间，不计算精确 ROI。

### 场景 G：M 矿船或运输船数量逐渐增加

规划器应同时评估：

- 当前总吞吐需求；
- M 船实体数量和调度压力；
- 是否可以用较少的 L 船替换；
- L 停靠和航线条件；
- L 船购买或自产条件；
- 当前区域的战损风险。

建议应优先表达“替换部分 M 船”，而不是在保留全部 M 船的同时无限增加 L 船。

### 场景 H：出现 Kha'ak 巢穴

当前归档事实中的巢穴、蜂巢和武器平台应直接提高风险等级。规划器应考虑：

- 提前从 M 矿运船转向 L；
- 缩短或绕开危险路线；
- 转移采矿区；
- 使用附近 NPC 来源替代远程运输；
- 暂停扩张并先处理巢穴；
- 配置 M 巡逻处理零散骚扰。

少量 M 巡逻船不能被描述为足以清除巢穴。

### 场景 I：存在海盗基地或海盗舰船

规划器应提高高价值货物和长距离运输的风险，并比较：

- 改走更安全路线；
- 缩短采购距离；
- 使用 L 运输船；
- 配置 M 巡逻或护航；
- 分散库存和采购批次；
- 改为附近供应或本地生产。

在普通海盗舰船尚未提取前，海盗基地只能作为风险信号，不能代表当前必然发生袭击。

### 场景 J：信息不足

如果玩家资金、NPC 库存、报价或实际舰队尚未提取，规划器仍可输出条件性步骤，例如：

```text
若 NPC A 当前有货且资金足够：采购并使用路线 R
若 NPC 无货：改用 NPC B 或建立临时自产
若现有运输能力不足：外购 M 运输船
若已经拥有可用运输船：优先重新分配现有舰船
```

这些条件必须作为明确分支展示，不能由代码顺序 fallback 隐式决定。

## 6. 最小威胁分级

规划器只做运营风险判断，不实现完整战斗模拟。

| 等级 | 可确认信号 | 运营影响 |
| --- | --- | --- |
| 低 | 无当前 Kha'ak/Xenon 站点或海盗基地，关系和路线无已知异常 | M 矿运船可建立早期闭环，不强制巡逻 |
| 中 | 潜在蜂巢来源、海盗基地、敌对阵营路线或少量敌舰 | M 船偏短途，评估 L 船、绕行和少量 M 巡逻 |
| 高 | 当前存档明确存在巢穴、蜂巢、武器平台或路线附近多项敌对事实 | 不让 M 船成为唯一方案，优先 L、绕行、转移、附近采购或先处理威胁 |

输出必须标注风险证据来自：

- 当前存档实际巢穴；
- 静态地图潜在来源；
- 海盗基地；
- 阵营关系；
- 普通敌对舰船；
- 或因数据缺失而无法确认。

## 7. 需要补充的存档提取

### 7.1 第一优先级：玩家经济与 NPC 交易

需要从真实原始存档样本确认并提取：

- 玩家资金；
- NPC station cargo/storage；
- NPC trade offer；
- 买卖方向；
- 数量；
- 动态价格；
- 报价有效状态；
- NPC 船厂可购船只和价格。

这些数据决定“出售盈余 → NPC 采购”的建议是否真实可执行。

### 7.2 第二优先级：普通舰船

运营规划所需的最小字段包括：

- 唯一标识；
- 舰船宏和型号；
- owner；
- M/L/XL 级别；
- 静态用途；
- 所在星区和位置；
- 玩家舰船的货物、任务和指派；
- 是否为残骸。

第一版不提取完整武器配置、实时护盾变化、攻击事件、AI 任务树或战斗模拟数据。

### 7.3 第三优先级：造船运行状态与风险历史

后续可补充：

- 玩家船厂队列；
- 船厂空闲能力；
- 攻击和警报记录；
- 舰船损失历史；
- 路线遭遇记录。

只有真实需求出现后才扩展这些数据，不为完整模拟预先建模。

### 7.4 预期代码影响面

存档字段补充将影响：

- `rust-parser/src/model.rs`；
- `rust-parser/src/core.rs`；
- `src/workers/saveParser.worker.ts`；
- `src/types/saveArchive.ts`；
- `src/workers/saveParser.post.ts`；
- `src/store/useSaveStore.ts`；
- Rust parser 测试和 `tests/unit/save-import/save-parser.spec.ts`。

修改 Rust parser 后才能执行 `npm run build-rust`；未修改 `rust-parser/src/*.rs` 时禁止运行该命令。

## 8. 架构约束

新功能严格采用：

```text
store → presenter → vue
```

Store 和领域逻辑负责：

- 存档事实和未知状态；
- 当前运营快照；
- 资源来源比较；
- 舰船阶段判断；
- 路线和威胁评估；
- 运营候选及操作依赖。

Presenter 负责：

- 为 UI 组装建议、步骤、前置条件、风险和未知项；
- 组织不同运营选择的对比结构；
- 生成 UI 使用的标签和说明。

Vue 只负责：

- 收集粗略意图和可选范围；
- 展示建议及差异；
- 触发 presenter 提供的行为。

不得新增 ViewModel、UI facade、adapter 或其他中间层。Vue 不得直接调用 store，store 不得输出为某个组件定制的展示结构。

第一版建议保持为派生的只读结果，不新增持久化字段。以后若需要保存已采纳的方案，给 `SaveBindingPlan` 等持久化类型增加字段时，必须同步更新 `useSaveBindingStore.ts` 的 `normalizeState()`。

## 9. 实施阶段

### 阶段 1：建立可验证的存档样本

- 收集包含玩家资金、普通玩家船、敌对船、NPC 库存和报价的真实原始存档片段；
- 为每类数据建立最小 parser fixture；
- 明确标签结构和数据生命周期；
- 区分“字段不存在”“值为零”和“当前 parser 未提取”。

验收：每项待提取事实都有真实样本，不能依据猜测设计字段。

### 阶段 2：补充经济和舰船事实

- 提取玩家资金；
- 提取 NPC 库存和动态报价；
- 提取玩家与敌对普通舰船的最小字段；
- 同步 Rust parser、JS fallback、TypeScript 类型、post-process 和归档标准化；
- 保持现有归档兼容。

验收：存档重新加载后字段不丢失；缺失数据不会被标准化成零值。

### 阶段 3：当前运营快照

- 汇总库存、预留、生产流、舰船、蓝图、船厂、NPC 供应和威胁；
- 明确区分存档事实、静态推断和未知数据；
- 通过 store 提供可复用领域结果。

验收：同一存档产生稳定快照；live 和 planning 状态不互相污染。

### 阶段 4：粗略意图与运营候选

- 支持扩建、新建、增加矿运能力、造舰队和安全运营意图；
- 推导下一里程碑的材料与能力需求；
- 并列比较库存、玩家产能、NPC 交易和新增自产；
- 输出操作步骤、前置条件、未知项和重新评估条件。

验收：玩家不输入目标产量也能获得可执行建议；资源缺口不会默认转为自产。

### 阶段 5：舰船阶段与 M/L 转换

- 判断外购、混合、自建阶段；
- 识别矿船、运输船和巡逻职责；
- 根据规模效率和战损风险判断 M 到 L 转换；
- 检查蓝图、船厂、L 停靠、材料和路线条件。

验收：不会在未知现有舰队时重复断言缺船；L 船建议包含明确前置条件。

### 阶段 6：威胁驱动的运营调整

- 使用实际巢穴、敌对站点、海盗基地、阵营关系和普通敌舰；
- 调整资源来源、航线、船型和建设顺序；
- 输出绕行、转移、附近采购、L 船、M 巡逻或暂停扩张等选择。

验收：不输出战斗胜率或安全保证；M 巡逻不会被描述成高风险区域的充分保护。

## 10. 测试重点

- 存档字段解析、归档、IndexedDB 恢复和旧版本兼容；
- 玩家资金为零、字段缺失和 parser 未提取三种状态的区分；
- NPC 有货、无货、报价过期和数据未知；
- 玩家已有舰船时不重复建议购买；
- 外购、混合、自建阶段判断；
- M 到 L 的规模触发与风险触发；
- 有无 Kha'ak 巢穴、海盗基地和敌对舰船时的建议差异；
- 扩建、新建和舰队意图不要求目标产量；
- live 与 planning 数据隔离；
- presenter 输出与 store 领域结果一致。

Live Production E2E 数据加载应复用：

- `tests/unified-e2e/live/helpers/loadLiveBindingFixture.ts`

## 11. 第一版明确不做

- 完整市场价格预测；
- NPC 行为和交易成功率模拟；
- 完整战斗与武器配置模拟；
- 精确舰船战损概率；
- AI 任务树和实时航行仿真；
- 在缺少动态价格、资金或库存时计算精确 ROI；
- 自动修改玩家规划或替玩家执行运营选择。

这些能力只有在存档事实和实际产品需求证明必要时再增加。
