# Test Tasks: ship-build

## 1. Unit Tests（Vitest）

- [ ] 1.1 过滤显示条件
  - [ ] 步骤 1：构造未选择 `class` 的输入。
  - [ ] 步骤 2：执行筛选逻辑。
  - [ ] 步骤 3：断言飞船列表应为空或不可见标记为 false。
- [ ] 1.2 `race` 与 `type` 为空时隐藏列表
  - [ ] 步骤 1：构造已选择 `class`、未选择 `race` 与 `type` 的输入。
  - [ ] 步骤 2：执行筛选逻辑。
  - [ ] 步骤 3：断言飞船列表不显示。
- [ ] 1.3 交集过滤规则
  - [ ] 步骤 1：构造已选择 `class`、`race`、`type` 的输入。
  - [ ] 步骤 2：执行筛选逻辑。
  - [ ] 步骤 3：断言结果满足 `class` + `race` + `type` 交集。
- [ ] 1.4 `type` 与 `class` 联动
  - [ ] 步骤 1：构造包含 `class: []` 的 `type` 选项集合。
  - [ ] 步骤 2：选择某个 `class` 并生成可选 `type`。
  - [ ] 步骤 3：断言仅返回匹配 `class` 的 `type`。
- [ ] 1.5 飞船名称本地化
  - [ ] 步骤 1：构造包含 `name` 与可翻译字段的飞船数据。
  - [ ] 步骤 2：调用 `x4i18n` 翻译逻辑生成展示名称。
  - [ ] 步骤 3：断言列表展示名称为翻译结果。
- [ ] 1.6 `race` 标签计数
  - [ ] 步骤 1：构造已选择 `class` 与若干 `type` 的输入。
  - [ ] 步骤 2：计算各 `race` 在当前 `type` 选择下的数量。
  - [ ] 步骤 3：断言 `race` 标签显示 `名称 (count)` 且计数正确。
- [ ] 1.7 `type` 标签计数
  - [ ] 步骤 1：构造已选择 `class` 与若干 `race` 的输入。
  - [ ] 步骤 2：计算各 `type` 在当前 `race` 选择下的数量。
  - [ ] 步骤 3：断言 `type` 标签显示 `名称 (count)` 且计数正确。

## 2. Web Integration / E2E

### 2.1 Bootstrapping & State

- [ ] 状态：船只建造视图
  - [ ] 步骤 1：启动应用并完成初始化。
  - [ ] 步骤 2：切换到“船只建造”视图。
  - [ ] 步骤 3：断言顶部“新建/保存/载入”按钮组存在且为绿色系主题。
  - [ ] 步骤 4：断言页面包含“选择船只”区域与下部三列占位布局。

- [ ] 切换：量化生产->船只建造
  - [ ] 步骤 1：进入“量化生产”视图。
  - [ ] 步骤 2：通过同级入口切换到“船只建造”。
  - [ ] 步骤 3：断言进入“船只建造”视图且状态正确。

### 2.2 Scenario Content

- [ ] 场景：未选择 `class` 不显示列表
  - [ ] 步骤 1：保持 `class` 未选择。
  - [ ] 步骤 2：断言飞船列表不显示。

- [ ] 场景：未选择 `race`/`type` 不显示列表
  - [ ] 步骤 1：选择某个 `class`。
  - [ ] 步骤 2：保持 `race` 与 `type` 为空。
  - [ ] 步骤 3：断言飞船列表不显示。

- [ ] 场景：选择 `class` + `race` 显示列表
  - [ ] 步骤 1：选择某个 `class`。
  - [ ] 步骤 2：选择一个 `race`。
  - [ ] 步骤 3：断言飞船列表显示且数据已过滤。

- [ ] 场景：选择 `class` + `type` 显示列表
  - [ ] 步骤 1：选择某个 `class`。
  - [ ] 步骤 2：选择一个 `type`。
  - [ ] 步骤 3：断言飞船列表显示且数据已过滤。

- [ ] 场景：`race` + `type` 同时选择取交集
  - [ ] 步骤 1：选择某个 `class`。
  - [ ] 步骤 2：同时选择 `race` 与 `type`。
  - [ ] 步骤 3：断言飞船列表仅包含交集结果。

- [ ] 场景：`type` 选项随 `class` 联动
  - [ ] 步骤 1：选择某个 `class`。
  - [ ] 步骤 2：打开 `type` 选项。
  - [ ] 步骤 3：断言仅出现匹配该 `class` 的 `type` 选项。

- [ ] 场景：飞船名称本地化展示
  - [ ] 步骤 1：切换至“船只建造”视图并显示飞船列表。
  - [ ] 步骤 2：断言列表中的飞船名称为本地化后的显示文本。

- [ ] 场景：`race` 标签显示计数
  - [ ] 步骤 1：选择 `class` 并选择一个或多个 `type`。
  - [ ] 步骤 2：观察 `race` 标签显示。
  - [ ] 步骤 3：断言 `race` 标签显示 `名称 (count)` 且数量符合当前 `type` 选择结果。

- [ ] 场景：`type` 标签显示计数
  - [ ] 步骤 1：选择 `class` 并选择一个或多个 `race`。
  - [ ] 步骤 2：观察 `type` 标签显示。
  - [ ] 步骤 3：断言 `type` 标签显示 `名称 (count)` 且数量符合当前 `race` 选择结果。
