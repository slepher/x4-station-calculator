# Test Tasks: ship-build

## 1. Unit Tests（Vitest）

- [x] 1.1 过滤显示条件
  - [x] 步骤 1：构造未选择 `class` 的输入。
  - [x] 步骤 2：执行筛选逻辑。
  - [x] 步骤 3：断言飞船列表应为空或不可见标记为 false。
- [x] 1.2 `race` 与 `type` 为空时隐藏列表
  - [x] 步骤 1：构造已选择 `class`、未选择 `race` 与 `type` 的输入。
  - [x] 步骤 2：执行筛选逻辑。
  - [x] 步骤 3：断言飞船列表不显示。
- [x] 1.3 交集过滤规则
  - [x] 步骤 1：构造已选择 `class`、`race`、`type` 的输入。
  - [x] 步骤 2：执行筛选逻辑。
  - [x] 步骤 3：断言结果满足 `class` + `race` + `type` 交集。
- [x] 1.4 `type` 与 `class` 联动
  - [x] 步骤 1：构造包含 `class: []` 的 `type` 选项集合。
  - [x] 步骤 2：选择某个 `class` 并生成可选 `type`。
  - [x] 步骤 3：断言仅返回匹配 `class` 的 `type`。
- [x] 1.5 飞船名称本地化
  - [x] 步骤 1：构造包含 `name` 与可翻译字段的飞船数据。
  - [x] 步骤 2：调用 `x4i18n` 翻译逻辑生成展示名称。
  - [x] 步骤 3：断言列表展示名称为翻译结果。
- [x] 1.6 `race` 标签计数
  - [x] 步骤 1：构造已选择 `class` 与若干 `type` 的输入。
  - [x] 步骤 2：计算各 `race` 在当前 `type` 选择下的数量。
  - [x] 步骤 3：断言 `race` 标签显示 `名称 (count)` 且计数正确。
- [x] 1.7 `type` 标签计数
  - [x] 步骤 1：构造已选择 `class` 与若干 `race` 的输入。
  - [x] 步骤 2：计算各 `type` 在当前 `race` 选择下的数量。
  - [x] 步骤 3：断言 `type` 标签显示 `名称 (count)` 且计数正确。

## 2. Web Integration / E2E

### 2.1 Bootstrapping & State

- [x] 状态：船只建造视图
  - [x] 步骤 1：启动应用并完成初始化。
  - [x] 步骤 2：切换到“船只建造”视图。
  - [x] 步骤 3：断言顶部“新建/保存/载入”按钮组存在且为绿色系主题。
  - [x] 步骤 4：断言页面包含“选择船只”区域与下部三列占位布局。

- [x] 切换：量化生产->船只建造
  - [x] 步骤 1：进入“量化生产”视图。
  - [x] 步骤 2：通过同级入口切换到“船只建造”。
  - [x] 步骤 3：断言进入“船只建造”视图且状态正确。

### 2.2 Scenario Content

- [x] 场景：未选择 `class` 不显示列表
  - [x] 步骤 1：保持 `class` 未选择。
  - [x] 步骤 2：断言飞船列表不显示。

- [x] 场景：未选择 `race`/`type` 不显示列表
  - [x] 步骤 1：选择某个 `class`。
  - [x] 步骤 2：保持 `race` 与 `type` 为空。
  - [x] 步骤 3：断言飞船列表不显示。

- [x] 场景：选择 `class` + `race` 显示列表
  - [x] 步骤 1：选择某个 `class`。
  - [x] 步骤 2：选择一个 `race`。
  - [x] 步骤 3：断言飞船列表显示且数据已过滤。

- [x] 场景：选择 `class` + `type` 显示列表
  - [x] 步骤 1：选择某个 `class`。
  - [x] 步骤 2：选择一个 `type`。
  - [x] 步骤 3：断言飞船列表显示且数据已过滤。

- [x] 场景：`race` + `type` 同时选择取交集
  - [x] 步骤 1：选择某个 `class`。
  - [x] 步骤 2：同时选择 `race` 与 `type`。
  - [x] 步骤 3：断言飞船列表仅包含交集结果。

- [x] 场景：`type` 选项随 `class` 联动
  - [x] 步骤 1：选择某个 `class`。
  - [x] 步骤 2：打开 `type` 选项。
  - [x] 步骤 3：断言仅出现匹配该 `class` 的 `type` 选项。

- [x] 场景：飞船名称本地化展示
  - [x] 步骤 1：切换至“船只建造”视图并显示飞船列表。
  - [x] 步骤 2：断言列表中的飞船名称为本地化后的显示文本。

- [x] 场景：`race` 标签显示计数
  - [x] 步骤 1：选择 `class` 并选择一个或多个 `type`。
  - [x] 步骤 2：观察 `race` 标签显示。
  - [x] 步骤 3：断言 `race` 标签显示 `名称 (count)` 且数量符合当前 `type` 选择结果。

- [x] 场景：`type` 标签显示计数
  - [x] 步骤 1：选择 `class` 并选择一个或多个 `race`。
  - [x] 步骤 2：观察 `type` 标签显示。
  - [x] 步骤 3：断言 `type` 标签显示 `名称 (count)` 且数量符合当前 `race` 选择结果。
