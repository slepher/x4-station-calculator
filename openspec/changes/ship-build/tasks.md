# Tasks: ship-build

## 1. 视图入口与按钮

- [x] 1.1 新增“船只建造”同级视图入口与切换逻辑。
- [x] 1.2 复用“新建/保存/载入”按钮组位置与结构，新增绿色系主题样式。

## 2. 页面布局

- [x] 2.1 搭建上部“选择船只”筛选区域布局。
- [x] 2.2 搭建下部三列占位布局（配装 / 配装后船体属性 / 建造材料），风格对齐“量化生产”。

## 3. 飞船筛选与列表显示

- [x] 3.1 基于 `types/x4.ts` 构建 `class`/`race`/`type` 筛选选项。
- [x] 3.2 实现 `type` 选项随 `class` 联动过滤。
- [x] 3.3 实现列表显示条件：`class` 必选，且 `race` 或 `type` 至少选一个。
- [x] 3.4 实现过滤规则：`race` 与 `type` 同时选择时取交集。

## 4. 名称本地化

- [x] 4.1 飞船列表名称使用 `x4i18n` 翻译结果。
- [x] 4.2 为新增飞船类型补充 `x4i18n` 处理逻辑。

## 5. 类型/种族计数标签

- [x] 5.1 `race` 标签显示 `名称 (count)`，`count` 基于当前 `type` 选择计算。
- [x] 5.2 `type` 标签显示 `名称 (count)`，`count` 基于当前 `race` 选择计算。
