## 1. Store 顺序重排能力

- [x] 1.1 在 `useEmpireStore` 中确认并统一 `reorderStations` 行为（长度校验、ID 校验、仅重排不改 ID）
- [x] 1.2 补充重排后的激活态保持逻辑验证（`activeStationId` 不因重排改变）
- [x] 1.3 将重排后的变更纳入现有保存流程，确保保存后顺序可恢复

## 2. 标签栏拖拽交互

- [x] 2.1 在 `StationTabBar` 中仅对空间站标签启用拖拽，排除“帝国总览”标签
- [x] 2.2 配置拖拽视觉反馈样式（ghost/chosen/dragging），保证目标位置可辨识
- [x] 2.3 处理拖拽与点击冲突，避免拖动过程中误触发标签切换
- [x] 2.4 处理取消/无效拖拽场景，确保顺序与激活态不被破坏

## 3. 持久化与边界场景

- [x] 3.1 验证重排后立即刷新（未保存）与保存后刷新两种行为符合现有存储语义
- [x] 3.2 验证删除被重排过的分站后，标签顺序与激活站回退逻辑正确
- [x] 3.3 验证复制分站后与重排联用时，顺序和新分站激活行为正确

## 4. 单元测试实现

- [x] 4.1 为 `reorderStations` 添加合法重排测试（前移、后移）
- [x] 4.2 为 `reorderStations` 添加非法输入防御测试（长度不匹配、未知 ID）
- [x] 4.3 添加重排后 `activeStationId` 保持不变测试

## 5. Web 集成测试实现

- [x] 5.1 新增标签拖拽重排 E2E 用例（Mouse API）验证顺序变化
- [x] 5.2 新增“帝国总览固定首位”E2E 用例
- [x] 5.3 新增“保存并刷新后顺序保持”E2E 用例
- [x] 5.4 新增“取消拖拽不改顺序”E2E 用例
