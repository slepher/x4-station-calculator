# x4-map-resource 实施任务

- [x] 1. 右上资源筛选入口与面板布局
- [x] 1.1 在 `MapWorkbenchView` 右上角增加常驻资源筛选面板
- [x] 1.2 将面板拆分为资源 tag 区、配置区、命中候选框
- [x] 1.3 保持与左上搜索框共存，并在筛选激活时切换为右侧固定宽度资源栏与关闭按钮
- [x] 1.4 将右上角资源 tag 文案与下方配置区文案分层：
  - 右上角普通资源 tag 使用 `res.*` 短名
  - 右上角 `sunlight` tag 使用 `res.energycells` 短名
  - 下方配置区保持原有长 i18n
- [x] 1.5 将默认右上入口改为“资源 + 过滤图标”按钮，并挂在地图右上角工具层而不是外层独立列块

- [x] 2. 资源筛选状态与丰度索引
- [x] 2.1 基于 `regionyields.json` 构建每个资源独立的 yield 顺序索引
- [x] 2.2 建立按资源维度维护的选中态与最低丰度状态
- [x] 2.3 新选中资源时默认初始化为 `lowest`
- [x] 2.4 增加 `日光` 特殊筛选项，并按 `1.0 = 100%` 映射到输入框百分比
- [x] 2.5 拆分“面板配置状态”和“资源高亮状态”，确保关闭面板时不重置过滤配置

- [x] 3. 资源配置区交互
- [x] 3.1 为每个已选资源渲染独立最低丰度下拉
- [x] 3.2 当已选普通资源数量大于等于 `2` 时显示 `所有项` 批量下拉
- [x] 3.3 修改 `所有项` 时同步更新全部已选资源的最低丰度
- [x] 3.4 在资源配置下拉中禁用高于当前过滤可达上限的选项
- [x] 3.5 当当前值超过可达上限时显示警告但不自动改值

- [x] 4. 资源命中与候选排序
- [x] 4.1 基于 `maps.json` 的 sector `resources[]` 计算资源命中集合
- [x] 4.2 按已选资源 `level` 总和从高到低排序命中候选
- [x] 4.3 将命中候选数量限制为最多 `10` 个
- [x] 4.4 点击命中候选时复用搜索结果的聚焦与选中逻辑
- [x] 4.5 当仅启用日光筛选时按日光百分比排序候选
- [x] 4.6 在未选择任何 tag 时，按 `ore + silicon + methane + hydrogen + helium` 的 `level` 总和展示默认前 `10` 条候选

- [x] 5. 搜索与资源高亮并存
- [x] 5.1 拆分搜索高亮、资源高亮与选中态输入
- [x] 5.2 在地图渲染层实现 `选中 > 搜索 > 资源` 的样式优先级
- [x] 5.3 确保搜索结果与资源筛选结果互不裁剪
- [x] 5.4 在资源工作态下，将命中项内部改为首个筛选项颜色，未命中项内部透明
- [x] 5.5 关闭资源过滤面板时仅清理资源高亮数据，不影响搜索高亮与当前选中态
- [x] 5.6 在同一页面会话内重新打开资源过滤面板时恢复关闭前的配置内容
- [x] 5.7 调整右侧过滤区头部布局，确保关闭按钮在未选择与已选择状态下都不会遮挡资源 tag

- [x] 6. 构建验证
- [x] 6.1 完成实现后执行 `npm run build`
- [x] 6.2 若构建失败，修复后重新构建直至通过或记录 blocker
