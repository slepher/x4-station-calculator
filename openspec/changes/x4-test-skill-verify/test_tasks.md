# Test Tasks: x4-test-skill-verify

## 1 单元测试

- [ ] 1.1 最小合法文档通过性
  - [ ] 1.1.1 构造仅含四章标题且章节为空的 `test_tasks.md` 样本
  - [ ] 1.1.2 执行 verify 脚本核心校验入口
  - [ ] 1.1.3 断言结果为通过（期望 toBe(true)）

- [ ] 1.2 四章结构完整性校验
  - [ ] 1.2.1 构造缺失 `## 3` 的样本
  - [ ] 1.2.2 执行结构校验
  - [ ] 1.2.3 断言返回“章节缺失”类错误（期望 toContain('Chapter 3')）

- [ ] 1.3 顶层任务编号规则校验
  - [ ] 1.3.1 构造顶层使用 `1.1.1` 的非法样本
  - [ ] 1.3.2 执行编号校验
  - [ ] 1.3.3 断言识别为顶层编号非法（期望 toContain('x.x')）

- [ ] 1.4 子任务编号规则校验
  - [ ] 1.4.1 构造子任务缺失三段编号的非法样本
  - [ ] 1.4.2 执行子任务格式校验
  - [ ] 1.4.3 断言识别为子任务编号非法（期望 toContain('x.x.x')）

- [ ] 1.5 子任务连续递增校验
  - [ ] 1.5.1 构造 `1.2.1` 后直接 `1.2.3` 的样本
  - [ ] 1.5.2 执行递增连续性校验
  - [ ] 1.5.3 断言识别为编号不连续（期望 toContain('不连续')）

- [ ] 1.6 缩进层级健壮性校验
  - [ ] 1.6.1 构造 0/2/4 之外缩进（如 1 空格、3 空格）的样本
  - [ ] 1.6.2 执行结构校验
  - [ ] 1.6.3 断言识别为非法缩进（期望 toContain('缩进')）

- [ ] 1.7 禁用旧步骤格式校验
  - [ ] 1.7.1 构造包含 `- [ ] 步骤 1:` 的样本
  - [ ] 1.7.2 执行格式校验
  - [ ] 1.7.3 断言识别为旧格式禁用（期望 toContain('步骤')）

- [ ] 1.8 Bug 顶层格式校验
  - [ ] 1.8.1 构造 Chapter 4 使用 `### BUG-001` 标题式主体的样本
  - [ ] 1.8.2 执行 Bug 章节校验
  - [ ] 1.8.3 断言识别为非法 Bug 主体格式（期望 toContain('BUG-')）

- [ ] 1.9 非法章节内容容错校验
  - [ ] 1.9.1 构造章节内出现纯文本段落（非 checklist）的样本
  - [ ] 1.9.2 执行树结构校验
  - [ ] 1.9.3 断言返回可定位的行级错误（期望 toContain('第')）

- [ ] 1.10 状态标记兼容性校验
  - [ ] 1.10.1 构造包含 `[ ]`、`[x]`、`[✓]`、`[✗]` 混合状态的样本
  - [ ] 1.10.2 执行解析与校验
  - [ ] 1.10.3 断言解析过程不崩溃且错误输出稳定（期望 toBe(true)）

- [ ] 1.11 异常输入稳健性校验
  - [ ] 1.11.1 构造空文件、超长行、含异常符号的样本集
  - [ ] 1.11.2 批量执行校验入口
  - [ ] 1.11.3 断言脚本不抛未捕获异常（期望 toBe(true)）

- [ ] 1.12 错误输出可调试性校验
  - [ ] 1.12.1 构造多类错误并存样本
  - [ ] 1.12.2 执行校验并收集输出
  - [ ] 1.12.3 断言错误信息包含规则类别与定位线索（期望 toContain('line')）

- [ ] 1.13 回归稳定性校验
  - [ ] 1.13.1 准备一组历史合法样本与非法样本基线
  - [ ] 1.13.2 连续多次执行校验
  - [ ] 1.13.3 断言同一输入输出一致（期望 toBe(true)）

- [ ] 1.14 一级任务末尾子任务直接含期望
  - [ ] 1.14.1 构造一级任务最后子任务本身包含“期望”的样本
  - [ ] 1.14.2 执行一级任务末尾期望规则校验
  - [ ] 1.14.3 断言规则通过（期望 toBe(true)）

- [ ] 1.15 一级任务末尾子任务由第三级期望子项兜底
  - [ ] 1.15.1 构造一级任务最后子任务本身不含“期望”，但其全部第三级子项均含“期望”的样本
  - [ ] 1.15.2 执行一级任务末尾期望规则校验
  - [ ] 1.15.3 断言规则通过（期望 toBe(true)）

- [ ] 1.16 一级任务末尾期望负例
  - [ ] 1.16.1 构造一级任务最后子任务本身不含“期望”，且其第三级子项存在不含“期望”的样本
  - [ ] 1.16.2 执行一级任务末尾期望规则校验
  - [ ] 1.16.3 断言规则失败并可定位（期望 toContain('期望')）

- [ ] 1.17 具体值期望标注格式校验
  - [ ] 1.17.1 构造具体值期望样本：`再充延迟: 1 s #期望: ['1 s']`
  - [ ] 1.17.2 构造计数期望样本：`断言字段集合包含36项字段标签 #期望: [36]`
  - [ ] 1.17.3 执行格式校验并断言可识别（期望 toBe(true)）

- [ ] 1.18 UI 存在性期望规则校验
  - [ ] 1.18.1 构造 UI 存在性样本（例如“断言按钮可见 #期望: [true]”）
  - [ ] 1.18.2 执行格式校验
  - [ ] 1.18.3 断言使用统一 `#期望: [...]` 形式即可通过（期望 toBe(true)）

- [ ] 1.19 test-impl 顶层任务与四类 spec 映射校验
  - [ ] 1.19.1 构造 `test_tasks-N-<case-name>.md` 与 `test-unit/test-e2e/test-bug/test-bug-fix` 四类样例 spec
  - [ ] 1.19.2 执行 `validate_test_case_refs.py --mode=test --file ... --json`
  - [ ] 1.19.3 断言输出仅包含 `case/desc/error_code/error_msg` 且映射结果稳定（期望 toBe(true)）

- [ ] 1.20 一级编号到 case 描述编号映射校验
  - [ ] 1.20.1 构造顶层 `1.1/2.1/3.1/4.1` 与 case 名编号一致样本
  - [ ] 1.20.2 构造顶层编号与 case 名编号不一致样本
  - [ ] 1.20.3 断言不一致时返回 `CASE_MISSING` 且 `case` 指向顶层编号（期望 toContain('CASE_MISSING')）

- [ ] 1.21 二级/三级编号到注释编号映射校验
  - [ ] 1.21.1 构造二级或三级任务缺少对应注释标号样本
  - [ ] 1.21.2 执行映射校验
  - [ ] 1.21.3 断言返回 `COMMENT_MISSING` 且 `case` 指向缺失标号（期望 toContain('COMMENT_MISSING')）

- [ ] 1.22 仅二层任务的区间内容校验
  - [ ] 1.22.1 构造 `x.x.x` 注释存在但到下一同层标号前无实际内容样本
  - [ ] 1.22.2 执行区间内容校验
  - [ ] 1.22.3 断言返回 `BLOCK_CONTENT_MISSING` 且 `case` 为该二层标号（期望 toContain('BLOCK_CONTENT_MISSING')）

- [ ] 1.23 含三层任务的区间内容校验
  - [ ] 1.23.1 构造 `x.x.x.n` 注释存在但到下一同层标号前无实际内容样本
  - [ ] 1.23.2 执行区间内容校验
  - [ ] 1.23.3 断言返回 `BLOCK_CONTENT_MISSING` 且 `case` 为该三级标号（期望 toContain('BLOCK_CONTENT_MISSING')）

- [ ] 1.24 期望断言存在与值匹配校验
  - [ ] 1.24.1 构造含 `#期望: [...]` 但无断言样本
  - [ ] 1.24.2 构造含断言但断言值与 `#期望` 不一致样本
  - [ ] 1.24.3 断言分别返回 `EXPECT_ASSERTION_MISSING` 与 `EXPECT_VALUE_MISMATCH`（期望 toContain('EXPECT_')）

- [ ] 1.25 Chapter4 bug/bug-fix 路由校验
  - [ ] 1.25.1 构造 `修复前` 仅出现在 bug、`修复后` 仅出现在 bug-fix 的正例样本
  - [ ] 1.25.2 构造 `修复后` 出现在 bug 路由或 `修复前` 出现在 bug-fix 路由的反例样本
  - [ ] 1.25.3 断言反例返回 `CH4_ROUTE_CONFLICT` 并可定位到具体 `case`（期望 toContain('CH4_ROUTE_CONFLICT')）

- [ ] 1.26 Chapter4 修复后勾选例外校验
  - [ ] 1.26.1 构造“修复后项勾选、无 bug case、存在 bug-fix case”的样本
  - [ ] 1.26.2 构造“修复后项勾选、无 bug-fix case”的样本
  - [ ] 1.26.3 断言前者通过、后者返回 `CASE_MISSING`（期望 toBe(true)）

## 2 E2E 标准状态与状态迁移

## 3 E2E 测试场景

## 4 Bug 测试
