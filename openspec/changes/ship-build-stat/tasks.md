# Tasks: ship-build-stat

## 1. 中列属性双档位

- [ ] 1.1 在中列属性区增加 `简略/详细` 档位切换控件。
- [ ] 1.2 默认进入 `简略` 档位并展示截图 2 对齐字段。
- [ ] 1.3 切换到 `详细` 档位时展示截图 1 对齐字段（含占位值）。
- [ ] 1.4 在 `详细` 档位显示“数据源待接入”提示。
- [ ] 1.5 保证 `详细` 字段集合覆盖 `简略` 字段集合。

## 2. 高度策略调整

- [ ] 2.1 移除中列属性区固定高度限制。
- [ ] 2.2 移除已选详情区固定高度限制。
- [ ] 2.3 检查取消固定高度后桌面与移动端布局可读性。

## 3. 文案与定位

- [ ] 3.1 补充双档位与占位提示的 i18n 键（zh-CN/en）。
- [ ] 3.2 按截图字段补充中英文 label 键与单位文案。
- [ ] 3.3 为属性面板与档位按钮提供稳定 `data-testid`。

## 4. 构建验证

- [ ] 4.1 完成改动后执行 `npm run build` 并通过。
