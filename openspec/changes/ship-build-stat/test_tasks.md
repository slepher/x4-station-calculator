# Test Tasks: ship-build-stat

## 1 单元测试

- [ ] 档位默认状态

### 档位默认状态

#### 前提
- 构造已选飞船 `Heron Vanguard` 的船只建造属性区渲染数据

#### 步骤 1: 渲染已选飞船的船只建造属性区
- 调用属性区组件渲染函数

#### 步骤 2: 读取当前档位状态
- 获取当前档位状态值

#### 步骤 3: 断言默认档位为简略
- 断言档位状态值为 "simple" 或默认档位标识

---

- [ ] 档位切换行为

### 档位切换行为

#### 前提
- 构造已选飞船 `Heron Vanguard` 的船只建造属性区渲染数据

#### 步骤 1: 点击详细档位按钮
- 模拟点击"详细"档位切换按钮

#### 步骤 2: 断言属性列表切换为详细字段集合
- 断言返回的字段集合包含详细扩展字段

#### 步骤 3: 点击简略档位按钮
- 模拟点击"简略"档位切换按钮

#### 步骤 4: 断言属性列表切回简略字段集合
- 断言返回的字段集合为简略字段集合

---

- [ ] 简略字段对齐

### 简略字段对齐

#### 前提
- 构造已选飞船 `Heron Vanguard` 的船只建造属性区渲染数据

#### 步骤 1: 进入简略档位
- 设置档位为"简略"

#### 步骤 2: 断言包含截图2对应字段标签
- 断言字段集合包含：船体、护盾、速度

#### 步骤 3: 断言不出现仅属于详细扩展的字段标签
- 断言字段集合不包含：武器爆发输出值、武器持续性输出值、炮塔平均输出值

---

- [ ] 详细字段对齐

### 详细字段对齐

#### 前提
- 构造已选飞船 `Heron Vanguard` 的船只建造属性区渲染数据

#### 步骤 1: 进入详细档位
- 设置档位为"详细"

#### 步骤 2: 断言包含截图1对应字段标签
- 断言字段集合包含：船体、护盾、速度、助推、巡航、船员、仓储、武器爆发输出值、武器持续性输出值、炮塔平均输出值

#### 步骤 3: 断言覆盖简略字段集合
- 断言详细字段集合包含简略字段集合的所有字段

---

- [ ] 可计算字段真实值显示

### 可计算字段真实值显示

#### 前提
- 构造含已选引擎和已选护盾的飞船状态
- 引擎: `engine_whisper`
- 护盾: `shield_standard_01`

#### 步骤 1: 进入详细档位
- 设置档位为"详细"

#### 步骤 2: 断言可计算字段为非占位值
- 断言 `船体` 字段值为数字（非 `--` 或占位符）
- 断言 `护盾` 字段值为数字
- 断言 `速度` 字段值为数字
- 断言 `助推` 字段值为数字
- 断言 `巡航` 字段值为数字
- 断言 `船员` 字段值为数字
- 断言 `仓储` 字段值为数字

---

- [ ] 不可计算字段占位显示

### 不可计算字段占位显示

#### 前提
- 构造已选飞船 `Heron Vanguard` 状态（未选择武器）

#### 步骤 1: 进入详细档位
- 设置档位为"详细"

#### 步骤 2: 断言武器输出字段显示占位值
- 断言 `武器爆发输出值` 字段值为 `--` 或占位符
- 断言 `武器持续性输出值` 字段值为 `--` 或占位符
- 断言 `炮塔平均输出值` 字段值为 `--` 或占位符

#### 步骤 3: 断言显示待接入提示文案
- 断言存在"待接入"或类似提示文案

---

- [ ] 高度限制回归

### 高度限制回归

#### 前提
- 构造已选飞船 `Heron Vanguard` 的属性区渲染数据

#### 步骤 1: 渲染属性区与已选详情区
- 调用组件渲染函数

#### 步骤 2: 检查容器样式
- 获取属性区容器DOM样式
- 获取已选详情区容器DOM样式

#### 步骤 3: 断言不存在固定高度相关样式
- 断言样式中不包含 `h-48`、`72px`、`height: 固定值`

---

## 2 E2E 标准状态与状态迁移

- [ ] 状态: empty-ship-build

### 状态: empty-ship-build

#### 描述
- 初始状态，未选择任何飞船

---

- [ ] 状态: heron-vanguard-selected

### 状态: heron-vanguard-selected

#### 描述
- 已选择飞船 `Heron Vanguard`（`ship_tel_l_trans_container_02_a`）

#### 前提
- 已选择 class: teladi, type: freighter
- 已选择飞船: `Heron Vanguard` (ID: ship_tel_l_trans_container_02_a)

---

- [ ] 切换: empty-ship-build -> heron-vanguard-selected

### 切换: empty-ship-build -> heron-vanguard-selected

#### 描述
- 从未选飞船状态切换到已选 Heron Vanguard

#### 前提
- 处于空船只建造状态

#### 步骤 1: 选择筛选条件
- 选择 class: L
- 选择 race: teladi
- 选择 type: freighter

#### 步骤 2: 选择飞船
- 在列表中点击选择 `Heron Vanguard`

#### 步骤 3: 断言状态切换成功
- 断言中列属性区可见
- 断言已选详情区可见
- 断言选中飞船ID为 `ship_tel_l_trans_container_02_a`

---

- [ ] 切换: simple-mode -> detailed-mode

### 切换: simple-mode -> detailed-mode

#### 描述
- 从简略档位切换到详细档位

#### 前提
- 处于 `heron-vanguard-selected` 状态
- 当前档位为简略

#### 步骤 1: 点击详细档位按钮
- 点击标记为"详细"的档位切换按钮

#### 步骤 2: 断言档位切换成功
- 断言当前档位为"详细"
- 断言属性区显示详细字段集合

---

## 3 E2E 测试场景

- [ ] 空船只建造视图

### 空船只建造视图

- 前提: 状态 empty-ship-build

#### 步骤 1: 进入船只建造视图
- 启动应用并进入"船只建造"视图

#### 步骤 2: 断言初始状态
- 断言未选择任何飞船
- 断言筛选条件为空

---

- [ ] 中列属性区双档位渲染

### 中列属性区双档位渲染

- 前提: 状态 heron-vanguard-selected

#### 步骤 1: 定位档位切换按钮
- 查找中列属性区的档位切换按钮组

#### 步骤 2: 断言按钮可见
- 断言"简略"按钮可见
- 断言"详细"按钮可见

---

- [ ] 简略字段与截图2对齐

### 简略字段与截图2对齐

- 前提: 状态 heron-vanguard-selected, 档位 simple-mode

#### 步骤 1: 切换到简略档位
- 点击"简略"档位按钮

#### 步骤 2: 断言字段集合与截图2一致
- 断言显示字段: 船体、护盾、速度
- 断言字段数量为简略模式标准数量

#### 步骤 3: 断言不包含详细扩展字段
- 断言不显示: 武器爆发输出值、武器持续性输出值、炮塔平均输出值

---

- [ ] 详细字段与截图1对齐

### 详细字段与截图1对齐

- 前提: 状态 heron-vanguard-selected
- 前提: 切换 simple-mode -> detailed-mode

#### 步骤 1: 切换到详细档位
- 点击"详细"档位按钮

#### 步骤 2: 断言字段集合与截图1一致
- 断言显示字段包含: 船体、护盾、速度、助推、巡航、船员、仓储
- 断言显示字段包含: 武器爆发输出值、武器持续性输出值、炮塔平均输出值

#### 步骤 3: 断言覆盖简略字段
- 断言详细字段集合包含所有简略字段

---

- [ ] 详细档位真实值与占位并存

### 详细档位真实值与占位并存

- 前提: 状态 heron-vanguard-selected, 档位 detailed-mode, 已选引擎和护盾

#### 步骤 1: 切换到详细档位
- 点击"详细"档位按钮

#### 步骤 2: 断言可计算字段为真实值
- 断言 `船体` 字段显示具体数值
- 断言 `护盾` 字段显示具体数值
- 断言 `速度` 字段显示具体数值
- 断言 `助推` 字段显示具体数值
- 断言 `巡航` 字段显示具体数值
- 断言 `船员` 字段显示具体数值
- 断言 `仓储` 字段显示具体数值

#### 步骤 3: 断言不可计算字段为占位值
- 断言 `武器爆发输出值` 字段显示 `--` 或占位符
- 断言 `武器持续性输出值` 字段显示 `--` 或占位符
- 断言 `炮塔平均输出值` 字段显示 `--` 或占位符

#### 步骤 4: 断言显示待接入提示
- 断言武器输出字段区域显示"待接入"提示文案

---

- [ ] 取消固定高度限制

### 取消固定高度限制

- 前提: 状态 heron-vanguard-selected

#### 步骤 1: 检查属性区容器样式
- 获取中列属性区容器的CSS样式

#### 步骤 2: 检查已选详情区容器样式
- 获取已选详情区容器的CSS样式

#### 步骤 3: 断言均未使用固定高度
- 断言属性区容器样式不包含固定高度值
- 断言已选详情区容器样式不包含固定高度值
- 断言高度为自动或内容自适应
