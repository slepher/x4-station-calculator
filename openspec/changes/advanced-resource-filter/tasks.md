# advanced-resource-filter 实施任务

- [x] 1. 资源过滤模式拆分
- [x] 1.1 为地图资源过滤区增加 `简单 / 高级` Tab 容器
- [x] 1.2 将现有单组资源过滤逻辑收敛到独立的简单模式组件中
- [x] 1.3 新建高级模式组件，承载多 `tag` 组编辑与候选组结果
- [x] 1.4 建立简单模式与高级模式各自独立的状态保存与切换恢复

- [x] 2. 高级模式输入模型
- [x] 2.1 定义高级模式 `tagGroups`、`sunlightConstraint`、`jumpLimit` 状态结构
- [x] 2.1.1 增加 `allowTransit` checkbox 状态，并接入高级模式表单
- [x] 2.2 实现高级模式的组编辑交互，包括新增、删除和组内资源 tag 维护
- [x] 2.2.1 将每个 `tag` 组实现为“摘要态 / 原位展开编辑态”双态组块，不额外创建独立编辑区
- [x] 2.2.2 限制同一时间仅允许一个 `tag` 组展开编辑
- [x] 2.2.3 调整组块布局，使已选 `tag` 列表与 `编辑` 按钮尽量同排显示，超量时在组内自然换行
- [ ] 2.2.4 将摘要态资源 tag 改为使用对应资源色填充，并保持文字与边框可读性
- [x] 2.3 保持 `日光` 作为过滤条件可配置，但不进入评分数据结构
- [x] 2.3.1 将 `日光` 从高级模式全局条件收回到各 `tag` 组内部，并为每个启用日光的组保留独立阈值输入
- [x] 2.3.2 为组内多个普通资源恢复 `所有项` 丰度联动下拉与 `混合` 状态显示

- [x] 3. 高级模式候选计算
- [x] 3.1 计算每个资源星区命中的 `tag` 组集合
- [x] 3.1.1 为命中多个 `tag` 组的星区汇总其命中组对应的普通资源并集，用于高级模式饼图 fill
- [x] 3.2 基于 `N` 跳约束为每个共同中转核心收集可达资源星区
- [x] 3.2.1 按 `allowTransit` 决定共同中转核心候选池来源
- [x] 3.3 判断每个中转核心对应的资源星区集合是否覆盖全部 `tag` 组
- [x] 3.4 将资源星区集合相同的多个中转核心合并为同一候选组
- [x] 3.5 对候选组执行“严格子集删除”的极大候选去冗余

- [x] 4. 高级模式排序与展示
- [x] 4.1 为每个 `tag` 组计算“最佳星区的组内普通资源平均 level”组分数
- [x] 4.2 取所有组分数中的最低值作为候选最终分数
- [x] 4.3 确保 `日光` 不参与高级模式评分，即使某星区只有日光命中
- [x] 4.4 将高级模式结果区改为展示“资源星区集合 + 中转核心候选集合 + 分数”的候选组列表
- [x] 4.5 调整地图联动，使高级模式高亮与候选组语义一致，并复用现有 `SectorResourceFill` 协议
- [x] 4.5.1 调整候选选中交互，确保选中高级候选后地图仍显示资源饼图，不被单 sector 选中态覆盖
- [x] 4.5.2 将候选内资源星区与中转候选改为可点击 tag，并接入“必要时切换候选 + focus 星区”交互
- [x] 4.5.3 切换候选时清理旧的资源 `selected` 状态，并确保 `selected` 状态下仍渲染资源饼图
- [ ] 4.5.4 调整整条高级候选点击行为：对 `resourceSectorIds + hubCandidateSectorIds` 并集执行整体 fit，而不是聚焦单个 sector
- [ ] 4.5.5 将整体 fit 改为基于六边形实际外接 box 计算，并使用 `boxWidth/boxHeight * 1.25` 适配 viewport 求 scale
- [ ] 4.5.6 保持候选内资源星区 tag / 中转候选 tag 的单 sector focus 语义，不与整条候选点击混用
- [ ] 4.5.7 为资源星区 tag 增加组序号 badge，按当前组顺序从 `1` 开始展示命中组
- [ ] 4.5.8 为地图中被资源 fill 染色的资源星区六边形增加底部中央 group badge，并复用同一套编号语义
- [ ] 4.5.9 仅在资源星区显示 group badge，不为纯中转星区显示该标记

- [x] 5. 文案与交互一致性
- [x] 5.1 为 `简单 / 高级` Tab、组块原位展开编辑态和候选组结果补齐所需文案
- [x] 5.2 确保切换 Tab 时地图高亮、结果列表与当前模式一致，不泄漏另一模式状态
- [x] 5.3 为右侧过滤区接入符合页面风格的内部滚动条，并确保地图本身不出现额外滚动条
- [x] 5.4 在高级模式刷新结果时保持右侧过滤区滚动位置不变
- [x] 5.5 验证相同资源集合不同核心合并、以及互不全包含候选并列展示的交互语义
- [x] 5.6 将高级模式 `跳数` 输入改为与简单模式日光输入一致的 stepper 风格，并限制范围为 `1..5`
- [x] 5.7 将 `跳数` 与 `允许中转` 调整为同一行显示
- [ ] 5.8 将候选卡片结果区重构为 `资源 / 中转` 两行等宽类型胶囊布局，替代 `中转候选:` 说明标题
- [ ] 5.9 保持 `资源` 与 `中转` 类型胶囊尺寸一致，并使两行右侧 tag 列表起始线对齐

- [x] 6. 构建验证
- [x] 6.1 完成实现后执行 `npm run build`
- [x] 6.2 若构建失败，修复后重新构建直至通过或记录 blocker
