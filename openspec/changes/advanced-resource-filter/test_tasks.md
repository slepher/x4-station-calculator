# advanced-resource-filter 测试任务

## 1 单元测试

- [ ] 1.1 matchSectorToTagGroup: 组内 AND 语义判定
  - [ ] 1.1.1 在 mapAdvancedResourceFilter 模块中，对 matchSectorToTagGroup 函数，传入包含多个普通资源 tag 的组配置，验证当星区满足全部 tag 时返回 matched=true #期望: [matched=true]
  - [ ] 1.1.2 在 mapAdvancedResourceFilter 模块中，对 matchSectorToTagGroup 函数，传入包含多个普通资源 tag 的组配置，验证当星区仅满足部分 tag 时返回 matched=false #期望: [matched=false]

- [ ] 1.2 matchSectorToTagGroup: 日光条件判定
  - [ ] 1.2.1 在 mapAdvancedResourceFilter 模块中，对 matchSectorToTagGroup 函数，传入包含日光 tag 且 sunlightMinimum=120 的组配置，验证当星区日光值>=120 时返回 includesSunlight=true 且 matched=true #期望: [matched=true, includesSunlight=true]
  - [ ] 1.2.2 在 mapAdvancedResourceFilter 模块中，对 matchSectorToTagGroup 函数，传入包含日光 tag 且 sunlightMinimum=120 的组配置，验证当星区日光值<120 时返回 matched=false #期望: [matched=false]

- [ ] 1.3 buildAdvancedCandidates: 候选生成与合并
  - [ ] 1.3.1 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，传入两个不同中转核心但对应相同资源星区集合的场景，验证返回结果中这两个核心被合并到同一候选项 #期望: [hubCandidateSectorIds.length=2]
  - [ ] 1.3.2 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，传入一个资源星区集合为另一个严格子集的候选对，验证最终结果中子集候选被过滤 #期望: [candidates.length=1]

- [ ] 1.4 buildAdvancedCandidates: 评分计算
  - [ ] 1.4.1 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，传入多个 tag 组配置，验证每个候选的分数为各组最佳星区平均 level 的最小值 #期望: [score 等于预期最小值]
  - [ ] 1.4.2 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，传入仅包含日光命中的星区，验证该星区不参与评分计算 #期望: [score=0]

- [ ] 1.5 buildSectorGraph: 跨 cluster 连通
  - [ ] 1.5.1 在 mapAdvancedResourceFilter 模块中，对 buildSectorGraph 函数，传入包含 cluster_gates 的地图数据，验证返回的图中包含跨 cluster 的双向连通边 #期望: [graph 包含跨 cluster 邻居]

- [ ] 1.6 buildAdvancedCandidates: allowTransit 开关
  - [ ] 1.6.1 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，设置 allowTransit=true，验证中转核心候选可来自任意星区 #期望: [hubCandidateSectorIds 包含非资源星区]
  - [ ] 1.6.2 在 mapAdvancedResourceFilter 模块中，对 buildAdvancedCandidates 函数，设置 allowTransit=false，验证中转核心候选仅来自命中资源星区 #期望: [hubCandidateSectorIds 仅包含资源星区]

## 2 E2E 标准状态与状态迁移

- [ ] 2.1 状态: 简单模式
  - [ ] 2.1.1 在地图界面，对资源过滤面板，点击简单模式 Tab
  - [ ] 2.1.2 等待面板切换完成
  - [ ] 2.1.3 检查资源 tag 列表渲染完成
  - [ ] 2.1.4 验证资源 tag 列表可见且未选中任何 tag #期望: [所有 tag 为未选中态]

- [ ] 2.2 状态: 高级模式
  - [ ] 2.2.1 在地图界面，对资源过滤面板，点击高级模式 Tab
  - [ ] 2.2.2 等待面板切换完成
  - [ ] 2.2.3 检查 tag 组列表渲染完成
  - [ ] 2.2.4 验证第一个 tag 组处于展开编辑态 #期望: [expandedGroupId 存在]
  - [ ] 2.2.5 验证跳数默认值为 2 #期望: [jumpLimit=2]
  - [ ] 2.2.6 验证允许中转默认勾选 #期望: [allowTransit=true]

- [ ] 2.3 切换: 简单模式 -> 高级模式
  - [ ] 2.3.1 在地图界面，对资源过滤面板，在简单模式下选中若干资源 tag
  - [ ] 2.3.2 点击高级模式 Tab
  - [ ] 2.3.3 验证简单模式状态保留 #期望: [切换后简单模式 tag 选中态不变]

- [ ] 2.4 切换: 高级模式 -> 简单模式
  - [ ] 2.4.1 在地图界面，对资源过滤面板，在高级模式下配置多个 tag 组
  - [ ] 2.4.2 点击简单模式 Tab
  - [ ] 2.4.3 验证高级模式状态保留 #期望: [切换后高级模式 tag 组不变]

- [ ] 2.5 状态: tag组展开编辑态
  - [ ] 2.5.1 在地图界面，对高级模式资源过滤面板，定位到某个 tag 组
  - [ ] 2.5.2 点击该组的编辑按钮
  - [ ] 2.5.3 检查组块展开动画完成
  - [ ] 2.5.4 验证该组原位展开并显示资源 tag 选择器 #期望: [组处于展开态]

- [ ] 2.6 状态: 高级候选选中态
  - [ ] 2.6.1 在地图界面，对高级模式资源过滤面板，配置 tag 组并点击刷新
  - [ ] 2.6.2 等待候选列表渲染完成
  - [ ] 2.6.3 检查第一个候选的选中状态
  - [ ] 2.6.4 验证第一个候选默认选中 #期望: [第一个候选有 selected 类]

## 3 E2E 测试场景

- [ ] 3.1 Case: Tab 切换保持状态独立
  - [ ] 3.1.1 状态: 简单模式
  - [ ] 3.1.2 选中 ore 和 silicon 两个资源 tag，记录候选结果
  - [ ] 3.1.3 切换: 简单模式 -> 高级模式
  - [ ] 3.1.4 在高级模式下，添加两个 tag 组，组1包含 ore，组2包含 silicon，点击刷新
  - [ ] 3.1.5 切换: 高级模式 -> 简单模式
  - [ ] 3.1.6 验证简单模式下 ore 和 silicon 仍然选中 #期望: [ore 和 silicon 仍为选中态]
  - [ ] 3.1.7 切换: 简单模式 -> 高级模式
  - [ ] 3.1.8 验证高级模式下之前配置的两个 tag 组仍然存在 #期望: [tag 组数量=2]

- [ ] 3.2 Case: 多 tag 组 AND 语义命中
  - [ ] 3.2.1 状态: 高级模式
  - [ ] 3.2.2 添加第一个 tag 组并选中 ore (丰度 medium)
  - [ ] 3.2.3 添加第二个 tag 组并选中 silicon (丰度 high)
  - [ ] 3.2.4 点击刷新，验证候选结果中的资源星区集合必须同时覆盖两个组 #期望: [候选资源星区满足 ore medium AND silicon high]
  - [ ] 3.2.5 验证候选分数为各组最佳星区平均 level 的最小值 #期望: [分数计算正确]

- [ ] 3.3 Case: 单星区覆盖多组
  - [ ] 3.3.1 状态: 高级模式
  - [ ] 3.3.2 添加第一个 tag 组并选中 ore (丰度 lowest)
  - [ ] 3.3.3 添加第二个 tag 组并选中 silicon (丰度 lowest)
  - [ ] 3.3.4 点击刷新，验证若某星区同时满足两个组，则该星区可覆盖全部组 #期望: [存在单星区覆盖多组的候选]
  - [ ] 3.3.5 验证地图上该星区的饼图切片为两组资源的并集 #期望: [饼图包含 ore 和 silicon 切片]

- [ ] 3.4 Case: 日光条件参与过滤不参与评分
  - [ ] 3.4.1 状态: 高级模式
  - [ ] 3.4.2 添加一个 tag 组并选中 ore 和日光，设置日光阈值为 150
  - [ ] 3.4.3 点击刷新，验证候选中的资源星区必须同时满足 ore 条件和日光条件 #期望: [资源星区日光值>=150]
  - [ ] 3.4.4 验证评分仅基于 ore 的 level 计算，日光不参与 #期望: [分数仅与 ore level 相关]

- [ ] 3.5 Case: 允许中转开关影响核心候选池
  - [ ] 3.5.1 状态: 高级模式
  - [ ] 3.5.2 配置两个 tag 组分别要求不同资源，设置跳数为 2
  - [ ] 3.5.3 勾选允许中转，点击刷新，验证中转核心候选可来自任意星区
  - [ ] 3.5.4 取消勾选允许中转，点击刷新，验证中转核心候选仅来自命中的资源星区 #期望: [核心候选池变化]
  - [ ] 3.5.5 验证两种模式下候选数量可能不同 #期望: [候选数量差异反映核心池限制]

- [ ] 3.6 Case: 跳数约束限制可达范围
  - [ ] 3.6.1 状态: 高级模式
  - [ ] 3.6.2 配置 tag 组，设置跳数为 1
  - [ ] 3.6.3 点击刷新，记录候选结果
  - [ ] 3.6.4 将跳数改为 3，再次刷新，验证候选资源星区范围扩大 #期望: [候选数量增加或持平]
  - [ ] 3.6.5 验证跳数输入限制在 1-5 范围 #期望: [输入超出范围时被 clamp]

- [ ] 3.7 Case: 候选卡片交互
  - [ ] 3.7.1 状态: 高级候选选中态
  - [ ] 3.7.2 点击候选卡片中的资源星区 tag，验证地图 focus 到该星区 #期望: [地图视图移动到对应星区]
  - [ ] 3.7.3 点击另一候选的资源星区 tag，验证当前候选切换且地图 focus 到该星区 #期望: [候选选中态切换]
  - [ ] 3.7.4 点击当前候选的中转核心 tag，验证候选不切换但地图 focus 到该星区 #期望: [选中候选不变]

- [ ] 3.8 Case: 组内丰度联动
  - [ ] 3.8.1 状态: tag组展开编辑态
  - [ ] 3.8.2 验证存在所有项联动下拉
  - [ ] 3.8.3 将所有项下拉改为 high，验证 ore 和 silicon 的丰度都变为 high #期望: [两个资源丰度同步更新]
  - [ ] 3.8.4 单独将 ore 改为 medium，验证所有项显示混合状态 #期望: [所有项下拉显示"混合"]

- [ ] 3.9 Case: 极大候选去冗余
  - [ ] 3.9.1 状态: 高级模式
  - [ ] 3.9.2 配置 tag 组使得存在子集关系
  - [ ] 3.9.3 点击刷新，验证资源星区集合被其他候选严格全包含的候选不出现在结果中 #期望: [无严格子集候选]
  - [ ] 3.9.4 验证互不全包含的候选同时保留 #期望: [互不包含候选共存]

- [ ] 3.10 Case: 刷新后滚动位置保持
  - [ ] 3.10.1 状态: 高级模式
  - [ ] 3.10.2 配置 tag 组并刷新生成多个候选
  - [ ] 3.10.3 滚动候选列表到中部位置，记录 scrollTop 值
  - [ ] 3.10.4 修改 tag 组配置，点击刷新，验证滚动位置保持不变 #期望: [scrollTop 与刷新前相近]

- [ ] 3.11 Case: 过滤区独立滚动
  - [ ] 3.11.1 状态: 高级模式
  - [ ] 3.11.2 添加多个 tag 组使内容超过可视高度
  - [ ] 3.11.3 在过滤区内滚动，验证仅过滤区内部滚动，地图区域不出现滚动条 #期望: [地图区域无额外滚动条]

- [ ] 3.12 Case: 跨 cluster 候选生成
  - [ ] 3.12.1 状态: 高级模式
  - [ ] 3.12.2 配置 tag 组要求分布在不同 cluster 的资源
  - [ ] 3.12.3 设置跳数足够大以允许跨 cluster 连通
  - [ ] 3.12.4 勾选允许中转，点击刷新，验证跨 cluster 的候选组合进入结果 #期望: [存在跨 cluster 候选]

- [ ] 3.13 Case: 简单模式资源过滤
  - [ ] 3.13.1 状态: 简单模式
  - [ ] 3.13.2 选中 ore 和 silicon 两个资源 tag
  - [ ] 3.13.3 验证候选列表仅显示同时包含这两个资源的星区 #期望: [候选星区包含 ore 和 silicon]
  - [ ] 3.13.4 验证候选按分数排序 #期望: [分数递减]

- [ ] 3.14 Case: 简单模式日光过滤
  - [ ] 3.14.1 状态: 简单模式
  - [ ] 3.14.2 选中日光 tag 并设置阈值
  - [ ] 3.14.3 验证候选仅包含满足日光条件的星区 #期望: [候选星区日光值满足阈值]

- [ ] 3.15 Case: 高级模式候选选中切换
  - [ ] 3.15.1 状态: 高级候选选中态
  - [ ] 3.15.2 点击另一个候选
  - [ ] 3.15.3 验证选中态切换 #期望: [新候选有 selected 类，旧候选无]

- [ ] 3.16 Case: 高级模式状态恢复
  - [ ] 3.16.1 状态: 高级模式
  - [ ] 3.16.2 配置多个 tag 组并刷新
  - [ ] 3.16.3 切换: 高级模式 -> 简单模式
  - [ ] 3.16.4 切换: 简单模式 -> 高级模式
  - [ ] 3.16.5 验证 tag 组配置保留 #期望: [tag 组数量不变]

- [ ] 3.17 Case: tag组编辑交互
  - [ ] 3.17.1 状态: tag组展开编辑态
  - [ ] 3.17.2 修改组内资源选择
  - [ ] 3.17.3 点击完成按钮收起编辑
  - [ ] 3.17.4 验证组块回到摘要态 #期望: [组显示更新后的 tag 标签]

## 4 Bug 测试

- [ ] 4.1 BUG-001: 跨 cluster 候选缺失
  - [ ] 4.1.1 在地图界面，对高级模式资源过滤面板，配置 tag 组要求 Black Hole Sun IV、Grand Exchange III/I/IV、Void of Opportunity 等跨 cluster 星区
  - [ ] 4.1.2 设置跳数为 2，勾选允许中转，点击刷新
  - [ ] 4.1.3 修复前验证跨 cluster 候选未进入结果 #期望: [无跨 cluster 候选]
  - [ ] 4.1.3 修复后验证跨 cluster 候选正确进入结果 #期望: [存在跨 cluster 候选]