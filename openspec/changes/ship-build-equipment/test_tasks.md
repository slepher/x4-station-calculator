# Test Tasks: ship-build-equipment

## 1. Unit Tests（Vitest）

- [ ] 1.1 候选过滤：全量装备来源
  - [ ] 步骤 1：准备包含多类型装备的 `equipments` 输入。
  - [ ] 步骤 2：调用候选过滤函数。
  - [ ] 步骤 3：断言候选仅来自 `equipments` 全量输入且不依赖 `group.equipments`。

- [ ] 1.2 候选过滤：`type + size` 匹配
  - [ ] 步骤 1：构造不同 `type/size` 的装备集合。
  - [ ] 步骤 2：以目标 connection 条件执行过滤。
  - [ ] 步骤 3：断言仅保留 `type` 与 `size` 同时匹配的装备。

- [ ] 1.3 候选过滤：`slotTags` 任意命中
  - [ ] 步骤 1：构造 connection tags 与 `equipment.slotTags` 存在单条交集的输入。
  - [ ] 步骤 2：执行过滤。
  - [ ] 步骤 3：断言存在任意 tag 命中即入选。

- [ ] 1.4 候选过滤：connection tags 为空
  - [ ] 步骤 1：构造 `connection.tags=[]` 的输入。
  - [ ] 步骤 2：执行过滤。
  - [ ] 步骤 3：断言按 `type + size` 返回候选。

- [ ] 1.5 双模式共用状态
  - [ ] 步骤 1：在标准模式下为多个 connection groups 分配装备。
  - [ ] 步骤 2：切换到简化模式。
  - [ ] 步骤 3：断言所有已配装结果保持一致。

- [ ] 1.6 简化模式批量分配
  - [ ] 步骤 1：构造同组多个 connection groups。
  - [ ] 步骤 2：在简化模式执行一次分配。
  - [ ] 步骤 3：断言该组全部 connection groups 同步更新为目标装备。

- [ ] 1.7 切换按钮灰态判定
  - [ ] 步骤 1：在同一 `slot.type` 下分配两种不同装备。
  - [ ] 步骤 2：计算简化模式可用性。
  - [ ] 步骤 3：断言切换按钮为禁用状态。

- [ ] 1.8 冲突解除恢复可用
  - [ ] 步骤 1：将同一 `slot.type` 的分配收敛为单一装备。
  - [ ] 步骤 2：重新计算简化模式可用性。
  - [ ] 步骤 3：断言切换按钮恢复可用。

- [ ] 1.9 简化模式主槽位聚合键为 `size + tags`
  - [ ] 步骤 1：构造同 `slot.type` 下 `M1/M4` 为 `standard`、`M2/M3` 为 `standard+missile` 的输入。
  - [ ] 步骤 2：生成简化模式聚合标签。
  - [ ] 步骤 3：断言存在两个 `M` 聚合标签（如 `M1/M2`），而非单一 `M`。

- [ ] 1.10 护盾按父槽位语义聚合
  - [ ] 步骤 1：构造 `L` 组含 `1` 个护盾、`M` 组含 `6` 个护盾的输入。
  - [ ] 步骤 2：在简化模式计算护盾聚合。
  - [ ] 步骤 3：断言 `L` 标签下护盾总数为 `1`、`M` 标签下为 `6`，且不会互相合并。

- [ ] 1.11 计数显示使用真实 `selected/total`
  - [ ] 步骤 1：构造一个分区总数大于 `1` 的 group。
  - [ ] 步骤 2：仅为其中部分 connection 赋值。
  - [ ] 步骤 3：断言区块计数显示为真实值（如 `2/8`），非固定 `0/1`。

- [ ] 1.12 候选装备名称使用 `nameId` i18n
  - [ ] 步骤 1：准备包含已翻译 `nameId` 的装备样本。
  - [ ] 步骤 2：生成候选列表。
  - [ ] 步骤 3：断言展示名称来自 i18n 文本，而不是静态 `name` 字段。

- [ ] 1.13 标签规则：`hittable/unhittable` 需额外标签
  - [ ] 步骤 1：构造包含 `hittable` 且另有标签（如 `standard`）的 connection 与装备输入。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言仅在命中 `hittable/unhittable` 且额外命中至少一个其他标签时入选。

- [ ] 1.14 标签规则：`hittable` 与 `unhittable` 互斥
  - [ ] 步骤 1：构造 connection 为 `hittable`、装备为 `unhittable` 的输入（反向亦然）。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言候选不被匹配。

- [ ] 1.15 Shield 不使用 `integrated` 特例
  - [ ] 步骤 1：构造 shield connection 与带 `integrated` 的装备输入。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言不会因为 `integrated` 特例而额外命中。

- [ ] 1.16 装备名称 i18n 一致性
  - [ ] 步骤 1：在标准与简化模式下渲染同一装备名称。
  - [ ] 步骤 2：获取两处显示文本。
  - [ ] 步骤 3：断言两处均为 i18n 文本且一致。

- [ ] 1.17 过滤 `noplayerblueprint=true`
  - [ ] 步骤 1：构造包含 `noplayerblueprint=true` 的装备样本。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言该类装备不会出现在候选列表。

## 2. Web Integration / E2E

### 2.1 Bootstrapping & State

- [ ] 状态：已选船进入配装区
  - [ ] 步骤 1：启动应用并切换到“船只建造”视图。
  - [ ] 步骤 2：选择一艘包含多个 slots/groups 的飞船。
  - [ ] 步骤 3：断言配装区可见且按 `slots -> groups` 显示。

- [ ] 状态：标准模式默认激活
  - [ ] 步骤 1：进入配装区初始页面。
  - [ ] 步骤 2：断言标准模式处于激活状态。
  - [ ] 步骤 3：断言 connection group 行支持单独选择。

- [ ] 状态：仅保留 Arsenal 候选视图
  - [ ] 步骤 1：进入配装区。
  - [ ] 步骤 2：检查顶部是否存在候选视图切换控件。
  - [ ] 步骤 3：断言 Nebula / Hangar / Tactical 不可见，且 Arsenal 视图正常渲染。

- [ ] 切换：标准模式->简化模式
  - [ ] 步骤 1：在无冲突分配下点击模式切换。
  - [ ] 步骤 2：断言界面切换为 group 聚合展示。
  - [ ] 步骤 3：断言已配数据未变化。

- [ ] 切换：简化模式->标准模式
  - [ ] 步骤 1：在简化模式完成一次批量分配。
  - [ ] 步骤 2：切换回标准模式。
  - [ ] 步骤 3：断言相关 connection groups 显示同一分配结果。

### 2.2 Scenario Content

- [ ] 场景：标准模式逐 connection group 分配
  - [ ] 步骤 1：在两个不同 connection group 分别选择装备。
  - [ ] 步骤 2：观察各行显示结果。
  - [ ] 步骤 3：断言两组分配可独立存在。

- [ ] 场景：简化模式按 group 批量分配
  - [ ] 步骤 1：切换至简化模式。
  - [ ] 步骤 2：对某 group 选择一项装备。
  - [ ] 步骤 3：断言该 group 下全部 connection groups 被同步配置。

- [ ] 场景：同一标签内同时提供主槽位与护盾选择
  - [ ] 步骤 1：进入一个包含炮塔与从属护盾的 group 标签。
  - [ ] 步骤 2：分别在“炮塔区”和“护盾区”进行选择。
  - [ ] 步骤 3：断言两块选择区独立生效且都归属于当前标签。

- [ ] 场景：候选过滤符合 `type + size + slotTags(any-hit)`
  - [ ] 步骤 1：打开某 connection group 的候选列表。
  - [ ] 步骤 2：检查候选项的类型、尺寸与标签命中关系。
  - [ ] 步骤 3：断言不满足条件的装备不出现。

- [ ] 场景：`hittable/unhittable` 需额外标签命中
  - [ ] 步骤 1：进入包含 `hittable`（或 `unhittable`）标签的 connection group。
  - [ ] 步骤 2：观察候选列表标签匹配情况。
  - [ ] 步骤 3：断言仅当额外标签也命中时才出现候选。

- [ ] 场景：`hittable` 与 `unhittable` 不互相匹配
  - [ ] 步骤 1：构造 `hittable` 连接与 `unhittable` 装备（或反向）场景。
  - [ ] 步骤 2：打开候选列表。
  - [ ] 步骤 3：断言该类装备不出现。

- [ ] 场景：shield 不使用 `integrated` 特例
  - [ ] 步骤 1：进入 shield 连接候选列表。
  - [ ] 步骤 2：观察带 `integrated` 标签的候选是否出现。
  - [ ] 步骤 3：断言未因 `integrated` 特例被额外纳入。

- [ ] 场景：候选视图名称标准化
  - [ ] 步骤 1：进入配装区候选视图。
  - [ ] 步骤 2：观察唯一候选视图名称。
  - [ ] 步骤 3：断言名称为标准化后的显示文本。

- [ ] 场景：装备名称在标准/简化模式一致
  - [ ] 步骤 1：标准模式选择并查看某装备名称。
  - [ ] 步骤 2：切换到简化模式并查看同一装备名称。
  - [ ] 步骤 3：断言两处名称一致且为 i18n 文本。

- [ ] 场景：`noplayerblueprint=true` 不参与候选
  - [ ] 步骤 1：进入任一候选列表。
  - [ ] 步骤 2：观察包含 `noplayerblueprint=true` 的装备是否出现。
  - [ ] 步骤 3：断言该类装备不显示。

- [ ] 场景：候选卡片不显示图片占位
  - [ ] 步骤 1：打开任一候选列表。
  - [ ] 步骤 2：观察候选卡片结构。
  - [ ] 步骤 3：断言仅显示文本信息，无图片占位块。

- [ ] 场景：简化模式同 size 不同 tags 拆分标签
  - [ ] 步骤 1：选择一艘存在同 size 不同 tags 槽组的飞船。
  - [ ] 步骤 2：切换到简化模式并查看 `M` 相关标签。
  - [ ] 步骤 3：断言显示为多个标签（如 `M1/M2`），每个标签对应不同 tags 语义。

- [ ] 场景：护盾统计不跨父槽位串组
  - [ ] 步骤 1：进入含 `L` 与 `M` 两类父槽位且护盾数量不同的飞船。
  - [ ] 步骤 2：切换到简化模式后分别查看 `L` 与 `M` 标签护盾区计数。
  - [ ] 步骤 3：断言各自计数与父槽位实际护盾数一致（例如 `1` 与 `6`）。

- [ ] 场景：同类型多装备导致简化切换置灰
  - [ ] 步骤 1：在同一 `slot.type` 下分配两种不同装备。
  - [ ] 步骤 2：观察简化模式切换控件。
  - [ ] 步骤 3：断言控件置灰且展示禁用原因提示。

- [ ] 场景：冲突解除后恢复切换
  - [ ] 步骤 1：将冲突类型收敛为同一装备。
  - [ ] 步骤 2：观察简化模式切换控件。
  - [ ] 步骤 3：断言控件恢复可点击。
