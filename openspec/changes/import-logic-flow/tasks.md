# Tasks: import-logic-flow

- [x] 1. 导入入口与布局调整
  - [x] 1.1 空间站页面将导入按钮移动到最右侧并右对齐。
  - [x] 1.2 帝国总览页面新增导入按钮并右对齐，样式与空间站入口一致。

- [x] 2. 导入弹窗与选择流程
  - [x] 2.1 新增逻辑组网导入选择弹窗（支持 Empire/Station 两种模式）。
  - [x] 2.2 Station 模式支持“方案 + 规划区”选择；Empire 模式支持“方案”选择。
  - [x] 2.3 空方案场景拦截提交并展示错误提示。

- [x] 3. 导入确认流程接入
  - [x] 3.1 Empire 导入接入 `SmartSaveDialog`，支持“保存并导入 / 放弃并导入”。
  - [x] 3.2 Station 导入接入独立确认弹窗，支持“导入为新空间站 / 覆盖当前空间站”。

- [x] 4. 导入映射能力实现
  - [x] 4.1 实现 `SavedFlowGroup -> plannedModules` 的聚合映射（仅 `manual` 节点）。
  - [x] 4.2 实现 `isolated` 资源过滤与锁定候选生成（仅 `container` 可锁）。
  - [x] 4.3 实现 Empire 批量导入：非空规划区建站、空规划区跳过。
  - [x] 4.4 实现 Station 导入：覆盖当前站与新建站两种写入策略。

- [x] 5. warning 汇总弹窗
  - [x] 5.1 设计 warning 数据结构与类型。
  - [x] 5.2 导入执行后统一展示单个 warning 汇总弹窗。

- [x] 6. 默认组名逻辑统一与 bug 修复
  - [x] 6.1 抽取默认组名计算为通用函数并在多处 UI 复用。
  - [x] 6.2 修复 store 自动写入 `group.name` 的行为，改为仅用户显式编辑时写入。

- [x] 7. 构建验证
  - [x] 7.1 执行 `npm run build`。
  - [x] 7.2 若构建失败，修复后重新执行直至通过或形成明确阻塞说明。

- [x] 8. 导入选择界面约束对齐（增量需求）
  - [x] 8.1 帝国导入切换为“加载帝国”界面形态，并通过直接改造当前导入模板实现（无删除入口）。
  - [x] 8.2 空间站导入保持一级菜单下拉；选中方案后，二级区按帝国同风格展示规划区。
  - [x] 8.3 空间站二级区仅展示可导入规划区，自动隐藏空规划区。
  - [x] 8.4 空间站二级区为每个规划区提供直接导入按钮。
  - [x] 8.5 当无可导入规划区时显示空态文案“该方案下暂无可导入的规划区”。
  - [x] 8.6 二级内容区不引入搜索、分页、排序能力。
  - [x] 8.7 回归确认导入映射/确认/warning 逻辑无行为变化。
  - [x] 8.8 帝国与空间站卡片补齐详情预览信息（含 `+N more...` 预览扩展提示）。
  - [x] 8.9 移除导入弹窗底部“继续”按钮触发流程，仅保留卡片内直接导入动作。

- [x] 9. 保存确认弹框判定与新建逻辑复用（增量需求）
  - [x] 9.1 梳理“新建按钮”当前保存确认判定条件并形成可复用入口。
  - [x] 9.2 导入按钮改为调用同一判定入口，不重复编写判断代码。
  - [x] 9.3 当判定逻辑位于 Vue 时，上提至 store 并由新建/导入共同调用。
  - [x] 9.4 帝国导入在“需要确认”时弹 `SmartSaveDialog`，不需要确认时直接导入。
  - [x] 9.5 回归验证：新建与导入在同状态下弹框结果逐条一致。

- [x] 10. dirty 判定口径修正（增量需求）
  - [x] 10.1 调整 dirty 快照/比较逻辑：`activeStationId` 变化不计入 dirty。
  - [x] 10.2 回归验证：仅执行站点页与帝国总览切换时，不触发“需要保存确认”。
  - [x] 10.3 回归验证：帝国业务数据变化仍可触发“需要保存确认”。
