# sector-link-calc 实施任务

- [x] 1. 子网范围缺口计算接入
- [x] 1.1 在缺口开关链路中接入 `getSectorNetworkComponent` 子网范围判定
- [x] 1.2 未分配星区空间站时返回空缺口结果
- [x] 1.3 保持现有产物显示/隐藏规则不变，仅替换计算范围

- [x] 2. 缺口明细模型改造
- [x] 2.1 建立按产物归并的统一明细结构
- [x] 2.2 注入本星区按空间站条目（产出/需求）
- [x] 2.3 注入其他星区按星区条目（产出/需求）
- [x] 2.4 将 A/B 结果合并到同一产物明细列表

- [x] 3. 中转站一跳外部贡献接入
- [x] 3.1 从纯函数边流中提取“本星区与连接星区一跳边”贡献
- [x] 3.2 按方向映射为输出/输入贡献并生成外星区明细条目
- [x] 3.3 将外部贡献与本地空间站贡献统一合并

- [x] 4. 统一口径公式落地
- [x] 4.1 存储需求使用固定 `12h` 缓冲口径
- [x] 4.2 运输需求沿用 `abs(netRate) * unitVolume`
- [x] 4.3 存储/运输明细同时展示外星区条目（名目为外星区名称）

- [x] 5. 纯函数接口适配
- [x] 5.1 评估并补充纯函数输出字段以降低 UI 侧推导复杂度
- [x] 5.2 保持 `container` 范围与默认 `distance=1` 的输入构建规范

- [x] 6. 构建验证
- [x] 6.1 完成实现后执行 `npm run build`
- [x] 6.2 若构建失败，修复后重复执行直到通过或记录阻塞

- [x] 7. 后续增补实现同步
- [x] 7.1 纯函数计算下沉到 `useEmpireStore` 缓存层，避免 tab 切换重复求解
- [x] 7.2 纯函数输入改为类别口径过滤；展示层保持 `netRate + 类别` 口径
- [x] 7.3 空星区回退：允许产物集从“连接且有站星区”并集补全
- [x] 7.4 修复边流 key 解析（`linkId` 含 `|` 时 from/to 串位）
- [x] 7.5 增加按星区粒度的纯函数日志输出
- [x] 7.6 仓储/运输外部条目文案改为输入/输出（输入绿，输出红）
- [x] 7.7 仓储/运输分组标题与内容同显同隐，空态不显示孤立标题
- [x] 7.8 中转 tab 显示条件改为“本星区无站且未连接到有站星区才隐藏”
- [x] 7.9 tab 栏超宽时增加左右横向滚动按钮
- [x] 7.10 空建筑区文案从“占位”改为正式空态提示
