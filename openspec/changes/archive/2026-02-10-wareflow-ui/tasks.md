# Implementation Tasks

- [x] **Refactor Header Title**: Update `header-title` CSS class in `StationWareFlowsDashboard.vue` to match `StationDashboard`.
- [x] **Refactor View Mode Switcher Container**: Update `view-mode-switcher` CSS class to use `bg-slate-900/60` and `border-slate-700/30`.
- [x] **Refactor View Mode Buttons**:
    - [x] Update `view-mode-btn` base styles (font size, padding, color).
    - [x] Update `view-mode-btn.active` to use Sky Blue theme (`bg-sky-500/20`, `text-sky-400`, shadow).
    - [x] Remove legacy "amber" styles.
