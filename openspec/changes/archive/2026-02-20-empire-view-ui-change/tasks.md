## 1. 标题与标签展示调整

- [x] 1.1 修改 `EmpireWareFlowsDashboard` 标题逻辑，资源/经济标题追加 `/h` 且不加空格
- [x] 1.2 修改 `StationWareFlowsDashboard` 标题逻辑，资源/经济标题追加 `/h` 且不加空格
- [x] 1.3 移除两个组件头部中的“每小时流量”标签展示

## 2. 帝国明细数量样式对齐

- [x] 2.1 重构 `EmpireWareFlow` 明细站点名称渲染，从字符串拼接改为“数量 + x + 名称”三段式结构
- [x] 2.2 对齐 `EmpireWareFlow` 与 `StationWareFlow.vue` 的明细数量样式（字体、`x` 样式、间距）
- [x] 2.3 确认 `stationCount` 数据来源保持不变，不修改任何计算逻辑

## 3. 回归验证

- [x] 3.1 验证帝国总览资源/经济视图标题文案与单位后缀
- [x] 3.2 验证空间站帝国资源区域资源/经济视图标题文案与单位后缀
- [x] 3.3 验证帝国总览与缺口明细中站点数量展示样式一致性

## 4. 测试资产同步

- [x] 4.1 更新/新增单元测试覆盖标题文案与明细数量渲染结构
- [x] 4.2 更新/新增 E2E 测试覆盖“无每小时流量标签”和“数量+x+名称”展示
- [x] 4.3 执行相关测试并同步更新 `test_tasks.md` 与 `ui_knowledge.md`
