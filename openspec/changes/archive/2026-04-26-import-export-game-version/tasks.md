# Tasks: import-export-game-version

## 1. 导出协议与导出 UI
- [x] 1.0 在实现说明中保留“导出源数据已跟随版本切换 storage key”的前提，不重复改造导出数据来源。
- [x] 1.1 扩展 `src/store/logic/importExport.ts` 的导出 payload，顶层写入 `game_vsn` 与 `beta`。
- [x] 1.2 更新 `src/components/StorageExportWizard.vue`，显示当前游戏版本信息。
- [x] 1.3 更新导出默认文件名生成逻辑，将 `game_vsn` 纳入文件名；beta 版本追加 `beta` 后缀。

## 2. 导入预检查与版本差异提示
- [x] 2.1 在导入解析阶段增加文件版本元数据读取，支持识别 `game_vsn` 与 `beta`。
- [x] 2.2 在导入配置界面显示文件版本与当前版本差异提示，但不阻止导入。
- [x] 2.3 保持对缺失 `game_vsn` / `beta` 的旧导出文件兼容，不因缺字段报错，并默认按 `8.0 stable` 解释。

## 3. 三模块失效数据清洗
- [x] 3.1 为 `x4_empire_data` 增加导入前模块存在性校验，移除当前版本不存在的站点模块引用。
- [x] 3.2 为 `x4_logic_flow_plans` 增加导入前模块存在性校验，移除当前版本不存在的 flow 模块节点。
- [x] 3.3 为 `x4_ship_blueprints` 增加导入前存在性校验，清洗失效的舰船、装备与仓储物品引用。
- [x] 3.4 为三模块清洗输出结构化统计与可展示摘要，供导入弹窗和最终 warning 共用。
- [x] 3.5 将正式导入流水线调整为 `coerce -> migrate -> sanitize -> apply`，避免清洗逻辑只停留在 UI 预览。

## 4. i18n 与模块命名统一
- [x] 4.1 梳理导入/导出与版本切换的模块命名来源，收敛到同一套共享 i18n key。
- [x] 4.2 将用户可见的 `Empire / 帝国` 模块命名统一替换为 `Sector / 星区`，不改动内部存储键。
- [x] 4.3 更新导入/导出弹窗、版本差异提示、清洗摘要与状态消息的相关 i18n 文案。

## 5. 验证
- [x] 5.1 增加单元测试，覆盖导出 payload 中 `game_vsn` / `beta` 输出与默认文件名生成。
- [x] 5.2 增加单元测试，覆盖导入预检查中的版本差异识别与旧文件兼容路径。
- [x] 5.3 增加单元测试，覆盖 Empire / Flow / Ship 三模块的失效引用清洗。
- [x] 5.4 增加单元测试或组件测试，覆盖导入/导出模块名称 i18n 统一与 `Sector / 星区` 命名替换。
