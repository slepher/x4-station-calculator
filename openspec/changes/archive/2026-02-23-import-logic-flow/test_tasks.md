# Test Tasks: import-logic-flow

## 1. Unit Tests（Vitest）

- [x] 1.1 默认组名通用函数
  - [x] 步骤 1：构造 `group.name` 为空且包含不同 `tier / isolated / order` 的输入。
  - [x] 步骤 2：调用默认组名函数。
  - [x] 步骤 3：断言返回最高 `tier` 的 `manual` 节点名称。
  - [x] 步骤 4：断言同 `tier` 时优先非 `isolated`，再按 `order`。
  - [x] 步骤 5：构造显式 `group.name`，断言返回显式名称。

- [x] 1.2 规划区映射函数
  - [x] 步骤 1：构造含 `manual` 与非 `manual` 节点的规划区输入。
  - [x] 步骤 2：调用 `SavedFlowGroup -> plannedModules` 映射函数。
  - [x] 步骤 3：断言仅 `manual` 节点进入结果。
  - [x] 步骤 4：断言相同 `moduleId` 正确聚合计数。
  - [x] 步骤 5：构造无 `manual` 输入并断言判定为空规划区。

- [x] 1.3 isolated 锁定过滤
  - [x] 步骤 1：构造 `isolated + container` 输入并执行过滤。
  - [x] 步骤 2：断言资源进入锁定集合。
  - [x] 步骤 3：构造 `isolated + 非container` 输入并执行过滤。
  - [x] 步骤 4：断言资源未被锁定，且产生忽略 warning。

- [x] 1.4 Empire 导入编排
  - [x] 步骤 1：构造“非空组+空组”混合方案并执行帝国导入编排。
  - [x] 步骤 2：断言仅非空组触发建站写入。
  - [x] 步骤 3：断言 warning 包含“空规划区跳过”。
  - [x] 步骤 4：构造全空方案并执行，断言导入被阻止。

- [x] 1.5 Store 行为回归
  - [x] 步骤 1：初始化 `group.name` 为空状态。
  - [x] 步骤 2：触发手动投放或节点变更。
  - [x] 步骤 3：断言 store 未自动写入推导名称。
  - [x] 步骤 4：触发用户显式重命名，断言此时才写入 `group.name`。

- [x] 1.6 dirty 判定口径（`activeStationId` 不计入）
  - [x] 步骤 1：构造“已保存且无业务数据改动”的帝国状态。
  - [x] 步骤 2：仅修改 `activeStationId`（站点 <-> 总览切换）并触发 dirty 判定。
  - [x] 步骤 3：断言 dirty 仍为 false。
  - [x] 步骤 4：修改帝国业务数据（如站点模块或帝国名称）后断言 dirty 变为 true。

## 2. Web Integration / E2E - Bootstrapping & State

说明：本章覆盖启动、数据预置、初始状态和状态切换；业务行为放在第 3 章。

- [x] 2.0 测试启动与数据预置
  - [x] 步骤 1：准备至少 3 套方案数据：`ilf_valid_single_group`、`ilf_mixed_groups`、`ilf_empty_plan`。
  - [x] 步骤 2：确认每套数据都满足 `SavedFlowPlansState` 结构并可被页面读取。
  - [x] 步骤 3：注入可复用方案数据并进入页面。
  - [x] 步骤 4：等待应用初始化完成。
  - [x] 步骤 5：确认空间站与帝国总览均可看到导入入口。

- [x] 2.1 Logic-Flow 主界面数据可用性（前置可展示性校验）
  - [x] 步骤 1：注入 fixture 方案数据并切换到 logic-flow 视图。
  - [x] 步骤 2：打开“载入流程方案”列表并确认可见至少 2 个方案。
  - [x] 步骤 3：加载方案 A，断言出现规划区且存在 manual 节点。
  - [x] 步骤 4：加载方案 B，重复步骤 3 断言。
  - [x] 步骤 5：断言页面无解析错误或异常提示。

- [x] 2.2 导入弹窗方案可见性（导入链路前置校验）
  - [x] 步骤 1：从空间站或帝国总览打开导入弹窗。
  - [x] 步骤 2：确认可切换至少 2 个方案。
  - [x] 步骤 3：在空间站场景切换方案后，确认规划区选择区域随方案更新且可交互。

- [x] 2.3 `状态：帝国已保存基线态` 验证
  - [x] 步骤 1：确保当前处于帝国数据上下文（可在帝国总览或任一空间站）。
  - [x] 步骤 2：准备至少一个有模块的空间站数据（可通过导入或手动编辑达成）。
  - [x] 步骤 3：点击工具栏 Save 并等待保存完成。
  - [x] 步骤 4：点击工具栏“新建”，断言不出现 SmartSaveDialog（进入 `状态：帝国已保存基线态`）。
  - [x] 步骤 5：若步骤 4 已触发新建帝国，重新准备后续用例所需的基础数据。

- [x] 2.4 `状态：帝国待保存更改态` 验证
  - [x] 步骤 1：先按 2.3 的步骤进入 `状态：帝国已保存基线态`。
  - [x] 步骤 2：对当前帝国做任意可见改动（如新增/修改空间站模块、重命名等）。
  - [x] 步骤 3：不点击 Save，保持当前页面。
  - [x] 步骤 4：点击工具栏“新建”，断言出现 SmartSaveDialog。
  - [x] 步骤 5：在 SmartSaveDialog 点击“取消/关闭（或按 Esc）”，断言弹框关闭且仍停留在当前帝国上下文（进入 `状态：帝国待保存更改态`）。

- [x] 2.5 `切换：帝国已保存基线态 -> 帝国待保存更改态` 验证
  - [x] 步骤 1：先按 2.3 的步骤进入并断言 `状态：帝国已保存基线态`。
  - [x] 步骤 2：对当前帝国做任意可见改动且不保存。
  - [x] 步骤 3：点击“新建”并断言出现 SmartSaveDialog，确认切换到 `状态：帝国待保存更改态`。

- [x] 2.6 `切换：帝国待保存更改态 -> 帝国已保存基线态` 验证
  - [x] 步骤 1：先按 2.4 的步骤进入并断言 `状态：帝国待保存更改态`。
  - [x] 步骤 2：点击 Save 并等待保存完成。
  - [x] 步骤 3：点击“新建”并断言不出现 SmartSaveDialog，确认切换到 `状态：帝国已保存基线态`。

- [x] 2.7 `状态：仅 activeStationId 变化仍为已保存基线态` 验证
  - [x] 步骤 1：进入 `状态：帝国已保存基线态`。
  - [x] 步骤 2：仅执行空间站页与帝国总览之间切换（触发 `activeStationId` 变化），不做其他业务改动。
  - [x] 步骤 3：点击“新建”，断言不出现 SmartSaveDialog（保持 `状态：帝国已保存基线态`）。

- [x] 2.8 `切换：activeStationId-only 变化不进入待保存态` 验证
  - [x] 步骤 1：进入 `状态：帝国已保存基线态` 并记录基准结果。
  - [x] 步骤 2：仅执行 `activeStationId` 变化（站点 <-> 总览切换）。
  - [x] 步骤 3：点击“新建”与“导入”分别断言不出现 SmartSaveDialog。

## 3. Web Integration / E2E - Scenario Content

说明：本章覆盖业务流程与用户可观察结果，不包含定位器/API/自动化实现细节。

- [x] 3.1 空间站页面入口与确认流程（覆盖导入）
  - [x] 步骤 1：在空间站进入导入流程，选择方案后在目标规划区卡片点击直接导入。
  - [x] 步骤 2：进入确认弹窗并选择“覆盖当前空间站”。
  - [x] 步骤 3：断言当前站模块先清空后写入导入结果。

- [x] 3.2 空间站“导入为新空间站”
  - [x] 步骤 1：在空间站导入流程选择有效方案后，通过规划区卡片直接导入触发流程。
  - [x] 步骤 2：在确认弹窗选择“导入为新空间站”。
  - [x] 步骤 3：断言空间站标签数量增加并自动切换到新站。

- [x] 3.3 帝国总览入口与 SmartSaveDialog 条件触发（依赖 `状态：帝国已保存基线态` / `状态：帝国待保存更改态`）
  - [x] 步骤 1：进入 `状态：帝国待保存更改态`，在帝国总览通过方案卡片触发导入。
  - [x] 步骤 2：断言出现 SmartSaveDialog，且包含“保存并导入 / 放弃并导入”。
  - [x] 步骤 3：分别执行两条路径并断言都能完成导入。
  - [x] 步骤 4：进入 `状态：帝国已保存基线态`，再次触发导入并断言不弹 SmartSaveDialog、直接执行导入。

- [x] 3.4 空方案阻止导入
  - [x] 步骤 1：进入空方案卡片并尝试触发卡片内导入动作。
  - [x] 步骤 2：断言提交被阻止（按钮被禁用）。
  - [x] 步骤 3：断言弹窗保持打开状态。

- [x] 3.5 空规划区跳过 + warning 汇总
  - [x] 步骤 1：在帝国总览导入混合方案（含空规划区）。
  - [x] 步骤 2：断言仅为非空规划区创建空间站。
  - [x] 步骤 3：断言出现 warning 汇总，并包含“空规划区已跳过”。

- [x] 3.6 非 container isolated 忽略 + warning 汇总
  - [x] 步骤 1：导入包含“非 container isolated”资源的方案。
  - [x] 步骤 2：断言该资源未进入锁定列表。
  - [x] 步骤 3：断言 warning 汇总包含“非 container 已忽略”。

- [x] 3.7 导入后不自动保存
  - [x] 步骤 1：完成一次导入但不手动保存。
  - [x] 步骤 2：刷新并重新进入目标站点。
  - [x] 步骤 3：断言导入结果未持久化。
  - [x] 步骤 4：重复导入后手动保存并刷新。
  - [x] 步骤 5：断言导入结果已持久化。

- [x] 3.8 导入入口对齐一致性回归（Bug #2）
  - [x] 步骤 1：在空间站记录导入按钮在工具栏中的水平与垂直位置。
  - [x] 步骤 2：切到帝国总览并记录导入按钮位置。
  - [x] 步骤 3：对比两者右对齐位置与基线，允许 1px 误差。
  - [x] 步骤 4：多次切换状态，断言按钮无明显跳位。

- [x] 3.9 增量需求：帝国导入切换为“加载帝国”形态（改当前模板）
  - [x] 步骤 1：从帝国总览进入导入流程。
  - [x] 步骤 2：断言导入界面呈现“加载帝国”形态。
  - [x] 步骤 3：断言该形态由当前导入模板改造而来（非新增并行模板路径）。
  - [x] 步骤 4：断言界面中不提供删除操作入口。

- [x] 3.10 增量需求：空间站一级下拉 + 二级同帝国形态展示
  - [x] 步骤 1：从空间站进入导入界面。
  - [x] 步骤 2：断言一级选择保持下拉形式。
  - [x] 步骤 3：选择一个方案后，断言二级区以帝国同风格内容区展示规划区列表（非下拉）。

- [x] 3.11 增量需求：空间站二级区隐藏空规划区
  - [x] 步骤 1：选择包含“非空规划区 + 空规划区”的方案。
  - [x] 步骤 2：断言二级区仅展示可导入规划区。
  - [x] 步骤 3：断言空规划区未出现在二级展示列表中。

- [x] 3.12 增量需求：空间站二级区直接导入按钮
  - [x] 步骤 1：选择一个含多个可导入规划区的方案。
  - [x] 步骤 2：在某个规划区项点击“直接导入”按钮。
  - [x] 步骤 3：断言以该规划区作为导入源进入后续导入流程。
  - [x] 步骤 4：断言无需先选中再点击统一继续按钮。

- [x] 3.13 增量需求：空间站二级区空态提示文案
  - [x] 步骤 1：选择一个无可导入规划区的方案。
  - [x] 步骤 2：断言显示空态文案“该方案下暂无可导入的规划区”。
  - [x] 步骤 3：断言此时无可触发导入的规划区操作项。

- [x] 3.14 增量需求：二级内容区能力边界
  - [x] 步骤 1：进入空间站导入界面并浏览二级内容区。
  - [x] 步骤 2：断言无搜索功能。
  - [x] 步骤 3：断言无分页功能。
  - [x] 步骤 4：断言无排序功能。

- [x] 3.15 增量需求：导入逻辑无变化回归
  - [x] 步骤 1：执行“覆盖当前空间站”导入并记录结果。
  - [x] 步骤 2：执行“导入为新空间站”并记录结果。
  - [x] 步骤 3：执行“帝国导入”并记录结果。
  - [x] 步骤 4：对比模块聚合结果与改造前基线一致。
  - [x] 步骤 5：对比跳过规则与 warning 汇总内容与改造前一致。

- [x] 3.16 增量需求：详情预览信息保留（帝国/空间站）
  - [x] 步骤 1：进入帝国导入界面，断言方案卡片展示更新时间、统计摘要与预览条目。
  - [x] 步骤 2：当预览条目超出展示上限时，断言显示 `+N more...`。
  - [x] 步骤 3：进入空间站导入界面并选中方案，断言规划区卡片展示节点统计与预览条目。
  - [x] 步骤 4：当预览条目超出展示上限时，断言显示 `+N more...`。

- [x] 3.17 增量需求：移除底部继续流程按钮
  - [x] 步骤 1：进入帝国导入界面，断言无底部“继续”按钮触发路径。
  - [x] 步骤 2：进入空间站导入界面，断言无底部“继续”按钮触发路径。
  - [x] 步骤 3：断言导入仅可通过卡片内直接导入动作触发。

- [x] 3.18 增量需求：导入与新建弹框判定逐条一致（依赖 `状态：帝国已保存基线态` / `状态：帝国待保存更改态`）
  - [x] 场景 A（`状态：帝国已保存基线态`）：新建与导入均不弹确认框。
    - [x] 步骤 1：进入 `状态：帝国已保存基线态`。
    - [x] 步骤 2：点击工具栏“新建”按钮并断言不出现 SmartSaveDialog。
    - [x] 步骤 3：点击工具栏“导入”按钮并触发导入，断言不出现 SmartSaveDialog。
  - [x] 场景 B（`状态：帝国待保存更改态`）：新建与导入均弹确认框。
    - [x] 步骤 1：通过 `切换：帝国已保存基线态 -> 帝国待保存更改态` 进入 `状态：帝国待保存更改态`。
    - [x] 步骤 2：点击“新建”并断言出现 SmartSaveDialog（保存并新建 / 放弃并新建）。
    - [x] 步骤 3：点击“导入”并断言出现 SmartSaveDialog（保存并导入 / 放弃并导入）。
  - [x] 场景 C（判定逻辑复用）：
    - [x] 步骤 1：记录场景 A/B 中“新建”与“导入”的弹框结果。
    - [x] 步骤 2：断言同一状态下两入口结果一致（S1 均不弹，S2 均弹），不存在单入口特例。

- [x] 3.19 Bug #3：新建/帝国导入不应把空帝国写入已保存列表
  - [x] 步骤 1：进入已保存帝国基线态并记录 `savedEmpires.list.length`。
  - [x] 步骤 2：点击“新建”（不触发保存确认路径）。
  - [x] 步骤 3：断言 `savedEmpires.list.length` 不增加。
  - [x] 步骤 4：在帝国导入执行卡片内直接导入（不触发保存确认），断言 `savedEmpires.list.length` 仍不增加。

## 4. 执行记录

- [x] 4.1 运行单元测试并记录结果。
- [x] 4.2 运行 E2E 测试并记录结果。
- [x] 4.3 失败项定位、修复后回归并更新结果。
  - [x] 2026-02-22 16:01 CST：单元测试（import-logic-flow）11 passed。
  - [x] 2026-02-22 16:01 CST：E2E（import-logic-flow）11 passed。
  - [x] 2026-02-22 16:26 CST：入口对齐专项回归 1 passed。
  - [x] 2026-02-22 21:30 CST：单元测试（import-logic-flow）11 passed。
  - [x] 2026-02-22 21:30 CST：`npm run build` 通过（用于 preview 模式 E2E）。
  - [x] 2026-02-22 21:30 CST：E2E（import-logic-flow）16 passed。
  - [x] 2026-02-22 23:38 CST：单元测试（import-logic-flow）11 passed。
  - [x] 2026-02-22 23:38 CST：E2E（import-logic-flow）22 passed。
  - [x] 2026-02-22：需求修正后，旧编号 2.11~2.20 已按新口径回归通过。
  - [x] 2026-02-23 02:08 CST：单元测试（import-logic-flow）11 passed。
  - [x] 2026-02-23 02:08 CST：E2E 状态专项（`状态：*`）2 passed。
  - [x] 2026-02-23 02:08 CST：E2E 切换专项（`切换：*`）2 passed。
  - [x] 2026-02-23 02:08 CST：E2E（import-logic-flow）26 passed。
  - [x] 2026-02-23 02:20 CST：单元测试（import-logic-flow）12 passed。
  - [x] 2026-02-23 02:20 CST：E2E 状态专项（`状态：*`）3 passed。
  - [x] 2026-02-23 02:20 CST：E2E 切换专项（`切换：*`）3 passed。
  - [x] 2026-02-23 02:20 CST：E2E（import-logic-flow）33 passed。
  - [x] 2026-02-23 02:47 CST：Bug #3 复现（`3.19`）先失败后修复通过。
    - [x] 修复前：`npx playwright test tests/e2e/import-logic-flow -g "3.19 Bug #3"` 1 failed。
    - [x] 修复后：`npx playwright test tests/e2e/import-logic-flow -g "3.19 Bug #3"` 1 passed。
  - [x] 2026-02-23 02:47 CST：E2E（import-logic-flow）34 passed。
  - [x] 2026-02-23 02:47 CST：单元测试（multi-station-empire）13 passed。

## 5. 需求变更迁移说明

- [x] 5.1 已移除不可执行旧步骤表述。
  - [x] 将“继续导入/选择后继续”改为“卡片内直接导入”触发方式。
- [x] 5.2 历史通过记录保留，但以第 3 章（`3.x`）为当前有效回归口径。
- [x] 5.3 旧流程相关项已迁移为当前基线复验项（`3.1`、`3.2`、`3.3`、`3.4`、`3.18`）。
  - [x] 2026-02-23 02:20 CST：`3.1`、`3.2`、`3.3`、`3.4`、`3.18` 已回归通过并完成勾选。
- [x] 5.4 已完成 E2E 章节拆分：
  - [x] 第 2 章：Bootstrapping & State
  - [x] 第 3 章：Scenario Content
