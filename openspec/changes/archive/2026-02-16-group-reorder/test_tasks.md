## Unit Tests

- [ ] **测试 1：moveGroupUp 函数**
  - 创建包含 3 个产线组的 mock 数据，顺序为 A、B、C
  - 调用 `moveGroupUp(B.id)`
  - 验证新顺序为 B、A、C

- [ ] **测试 2：moveGroupDown 函数**
  - 创建包含 3 个产线组的 mock 数据，顺序为 A、B、C
  - 调用 `moveGroupDown(A.id)`
  - 验证新顺序为 B、A、C

- [ ] **测试 3：边界情况 - 第一个产线组**
  - 创建包含 3 个产线组的 mock 数据，顺序为 A、B、C
  - 调用 `moveGroupUp(A.id)`
  - 验证顺序不变（仍为 A、B、C）

- [ ] **测试 4：边界情况 - 最后一个产线组**
  - 创建包含 3 个产线组的 mock 数据，顺序为 A、B、C
  - 调用 `moveGroupDown(C.id)`
  - 验证顺序不变（仍为 A、B、C）

## Web Integration Tests

- [ ] **测试 5：上箭头移动**
  - 创建 3 个产线组，记录初始顺序
  - 点击第二个产线组的上箭头
  - 验证第二个产线组移动到第一位

- [ ] **测试 6：下箭头移动**
  - 创建 3 个产线组，记录初始顺序
  - 点击第一个产线组的下箭头
  - 验证第一个产线组移动到第二位

- [ ] **测试 7：第一个产线组隐藏上箭头**
  - 创建 3 个产线组
  - 验证第一个产线组的上箭头不可见
  - 验证第一个产线组的下箭头可见

- [ ] **测试 8：最后一个产线组隐藏下箭头**
  - 创建 3 个产线组
  - 验证最后一个产线组的下箭头不可见
  - 验证最后一个产线组的上箭头可见

- [ ] **测试 9：单个产线组隐藏所有箭头**
  - 创建 1 个产线组
  - 验证上箭头和下箭头都不可见

- [ ] **测试 10：排序持久化**
  - 创建 3 个产线组
  - 使用箭头重新排列顺序
  - 刷新页面
  - 验证产线组顺序保持
