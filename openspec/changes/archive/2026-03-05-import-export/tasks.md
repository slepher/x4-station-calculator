# Tasks: import-export

## 1. 导入导出 UI 流程

- [x] 1.1 复用 TabBar 现有导入/导出按钮作为入口，不新增平行入口。
- [x] 1.2 实现导出按钮打开导出弹窗（独立 Vue 组件），不再一键直下。
- [x] 1.3 在导出弹窗展示模块统计（帝国/flow/ship + 条目数量）。
- [x] 1.4 在导出弹窗提供可编辑文件名输入框（默认系统生成文件名）。
- [x] 1.5 导出弹窗 footer 提供“取消/下载”操作，下载按钮为图标+文本样式。
- [x] 1.6 下载时按输入文件名导出 JSON，并自动补齐 `.json` 后缀。
- [x] 1.7 实现导入按钮文件上传界面（JSON 选择与基础校验）。
- [x] 1.8 实现上传后导入配置面板：
  - 覆盖/增量选项
  - 模块统计（帝国/flow/ship + 条目数量）
  - 模块级多选
- [x] 1.9 实现“覆盖模式默认全选可导入模块”的交互规则。

## 2. 导入导出数据契约与模块注册

- [x] 2.1 定义导入导出模块契约（模块 key、迁移函数、覆盖/增量写入、active 决策、刷新回调）。
- [x] 2.2 落地模块注册表，纳入 `x4_empire_data`、`x4_logic_flow_plans`、`x4_ship_blueprints` 三个模块。
- [x] 2.3 实现 Empire 迁移函数（v1->v2），并为 Logic Flow / Ship Build 增加 no-op 迁移入口。

## 3. 导入执行器与模式策略

- [x] 3.1 实现统一导入执行器，串联 `migrate -> apply mode -> active resolve -> refresh`。
- [x] 3.2 执行用户勾选模块子集导入，未勾选模块不处理。
- [x] 3.3 实现覆盖模式：清理当前模块数据并写入导入结果。
- [x] 3.4 实现增量模式：导入对象 ID 重生、引用回填、基于当前数据追加。

## 4. ActiveId 与 Empire 联动策略

- [x] 4.1 实现覆盖模式下的 `activeId` 直接更新规则与合法性回退。
- [x] 4.2 实现增量模式下“空 active 或空且不脏才更新”的规则。
- [x] 4.3 实现 Empire `activeId` 更新时的 `activeStationId` 同步更新。

## 5. 页面上下文刷新

- [x] 5.1 建立按页面上下文的刷新分发（production/flow/ship-build）。
- [x] 5.2 仅在 `activeId` 实际变化时触发刷新，避免无效重载。

## 6. 导出能力

- [x] 6.1 实现模块化导出载荷生成（包含模块数据与版本字段）。
- [x] 6.2 确保导出载荷可作为同版本系统的导入输入。

## 7. 构建验证

- [x] 7.1 执行 `npm run build`，确认改造后可正常编译。
