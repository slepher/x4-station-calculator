# Test Tasks: ship-build-equipment

## 0. 执行结果（2026-02-25）

- [x] 已创建并执行本 change 专属测试目录：
  - [x] `tests/unit/ship-build-equipment/ship-build-equipment.spec.ts`
  - [x] `tests/e2e/ship-build-equipment/ship-build-equipment.spec.ts`
- [x] 本轮执行汇总：
  - [x] `npx vitest run tests/unit/ship-build-equipment`：8 tests，8 passed
  - [x] `npx playwright test tests/e2e/ship-build-equipment`：25 tests，25 passed
- [ ] 迁移说明（2026-02-25）：标签匹配规则已从 `any-hit + hittable/unhittable` 特判迁移为统一 `ALL` 匹配；兼容性标签展示改为 6 标签白名单，旧断言需按新基线重跑。

## 1. Unit Tests（Vitest）

- [ ] 1.1 候选过滤：全量装备来源
  - [ ] 步骤 1：准备包含多类型装备的 `equipments` 输入。
  - [ ] 步骤 2：调用候选过滤函数。
  - [ ] 步骤 3：断言候选仅来自 `equipments` 全量输入且不依赖 `group.equipments`。

- [x] 1.2 候选过滤：`type + size` 匹配
  - [ ] 步骤 1：构造不同 `type/size` 的装备集合。
  - [ ] 步骤 2：以目标 connection 条件执行过滤。
  - [ ] 步骤 3：断言仅保留 `type` 与 `size` 同时匹配的装备。

- [ ] 1.3 候选过滤：`slotTags` 全量包含（ALL）匹配
  - [ ] 步骤 1：构造 `equipment.slotTags` 仅部分落在 `connection.tags` 的输入。
  - [ ] 步骤 2：执行过滤。
  - [ ] 步骤 3：断言仅当 `equipment.slotTags` 全量被 `connection.tags` 包含时才入选。

- [x] 1.4 候选过滤：connection tags 为空
  - [ ] 步骤 1：构造 `connection.tags=[]` 的输入。
  - [ ] 步骤 2：执行过滤。
  - [ ] 步骤 3：断言按 `type + size` 返回候选。

- [ ] 1.5 双模式共用状态
  - [ ] 步骤 1：在标准模式下为多个 connection groups 分配装备。
  - [ ] 步骤 2：切换到简化模式。
  - [ ] 步骤 3：断言所有已配装结果保持一致。

- [ ] 1.6 简化模式批量分配
  - [ ] 步骤 1：构造同组多个 connection groups。
  - [ ] 步骤 2：在简化模式执行一次分配。
  - [ ] 步骤 3：断言该组全部 connection groups 同步更新为目标装备。

- [x] 1.7 切换按钮灰态判定
  - [ ] 步骤 1：在同一 `slot.type` 下分配两种不同装备。
  - [ ] 步骤 2：计算简化模式可用性。
  - [ ] 步骤 3：断言切换按钮为禁用状态。

- [x] 1.8 冲突解除恢复可用
  - [ ] 步骤 1：将同一 `slot.type` 的分配收敛为单一装备。
  - [ ] 步骤 2：重新计算简化模式可用性。
  - [ ] 步骤 3：断言切换按钮恢复可用。

- [x] 1.9 简化模式主槽位聚合键为 `size + tags`
  - [ ] 步骤 1：构造同 `slot.type` 下 `M1/M4` 为 `standard`、`M2/M3` 为 `standard+missile` 的输入。
  - [ ] 步骤 2：生成简化模式聚合标签。
  - [ ] 步骤 3：断言存在两个 `M` 聚合标签（如 `M1/M2`），而非单一 `M`。

- [ ] 1.10 护盾按父槽位语义聚合
  - [ ] 步骤 1：构造 `L` 组含 `1` 个护盾、`M` 组含 `6` 个护盾的输入。
  - [ ] 步骤 2：在简化模式计算护盾聚合。
  - [ ] 步骤 3：断言 `L` 标签下护盾总数为 `1`、`M` 标签下为 `6`，且不会互相合并。

- [ ] 1.11 计数显示使用真实 `selected/total`
  - [ ] 步骤 1：构造总数为 `8` 的中型炮塔分区（例如 `M` 炮塔分区）。
  - [ ] 步骤 2：仅为其中部分 connection 赋值。
  - [ ] 步骤 3：断言区块计数显示为真实值（如 `2/8`），非固定 `0/1`。

- [ ] 1.12 候选装备名称使用 `nameId` i18n
  - [ ] 步骤 1：准备包含已翻译 `nameId` 的装备样本。
  - [ ] 步骤 2：生成候选列表。
  - [ ] 步骤 3：断言展示名称来自 i18n 文本，而不是静态 `name` 字段。

- [ ] 1.13 标签规则：`hittable/unhittable` 不做特判
  - [ ] 步骤 1：构造包含 `hittable/unhittable` 标签的 connection 与装备输入。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言 `hittable/unhittable` 仅作为普通标签参与 ALL 匹配，不存在额外分支条件。

- [ ] 1.14 兼容性标签展示：仅 6 标签白名单
  - [ ] 步骤 1：构造包含白名单内与白名单外标签混合的候选标签输入。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言兼容性标签栏只显示 6 标签白名单。

- [ ] 1.15 Shield 不使用 `integrated` 特例
  - [ ] 步骤 1：构造 shield connection 与带 `integrated` 的装备输入。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言不会因为 `integrated` 特例而额外命中。

- [ ] 1.16 装备名称 i18n 一致性
  - [ ] 步骤 1：在标准与简化模式下渲染同一装备名称。
  - [ ] 步骤 2：获取两处显示文本。
  - [ ] 步骤 3：断言两处均为 i18n 文本且一致。

- [x] 1.17 过滤 `noplayerblueprint=true`
  - [ ] 步骤 1：构造包含 `noplayerblueprint=true` 的装备样本。
  - [ ] 步骤 2：执行候选过滤。
  - [ ] 步骤 3：断言该类装备不会出现在候选列表。

- [ ] 1.18 兼容性标签展示：白名单过滤为空则隐藏整栏
  - [ ] 步骤 1：构造仅含非白名单标签的候选样本。
  - [ ] 步骤 2：渲染候选卡片并检查兼容性标签栏。
  - [ ] 步骤 3：断言该候选不渲染兼容性标签栏。

- [ ] 1.19 兼容性标签展示：文本来自 `slot_tags.json` i18n
  - [ ] 步骤 1：准备包含 `standard/advanced/xenon/mining/missile/highpower` 的候选样本与 `slot_tags.json` 映射。
  - [ ] 步骤 2：执行标签渲染逻辑。
  - [ ] 步骤 3：断言展示文本来自 `nameId` 翻译结果，而非原始 tag id。

- [ ] 1.20 类型约束：`x4.ts` 声明 slot tag 类型
  - [ ] 步骤 1：对 `slot_tags.json` 的消费代码执行类型检查。
  - [ ] 步骤 2：验证页面渲染链路使用 slot tag 类型而非裸字符串结构。
  - [ ] 步骤 3：断言 `tsc` 无类型错误。

## 2. Bootstrapping & State（E2E）

- [x] 2.1 状态：标准测试状态-大太刀
  - [ ] 步骤 1：启动应用并切换到“船只建造”视图。
  - [ ] 步骤 2：若当前已在“船只建造”且已选中其他飞船，先点击“更换飞船”返回选船列表。
  - [ ] 步骤 3：`class` 选择 `M`，`race` 选择 `terran`，`type` 选择“轻型护卫舰”。
  - [ ] 步骤 4：在结果列表选择“大太刀”。
  - [ ] 步骤 5：断言进入配装区，且可见引擎/护盾/武器/炮塔四类配装区块。

- [ ] 2.2 状态：标准测试状态-大阪
  - [ ] 步骤 1：启动应用并切换到“船只建造”视图。
  - [ ] 步骤 2：若当前已在“船只建造”且已选中其他飞船，先点击“更换飞船”返回选船列表。
  - [ ] 步骤 3：`class` 选择 `L`，`race` 选择 `terran`，`type` 选择“驱逐舰”。
  - [ ] 步骤 4：在结果列表选择“大阪”。
  - [ ] 步骤 5：断言进入配装区，且 `group_back_down_mid`、`group_back_mid_up`、`group_down_mid_left` 三个炮塔分组与计数区域可见。

- [ ] 2.3 状态：标准测试状态-苍鹭
  - [ ] 步骤 1：启动应用并切换到“船只建造”视图。
  - [ ] 步骤 2：若当前已在“船只建造”且已选中其他飞船，先点击“更换飞船”返回选船列表。
  - [ ] 步骤 3：`class` 选择 `L`，`race` 选择 `teladi`，`type` 选择“货船”。
  - [ ] 步骤 4：在结果列表选择“苍鹭”。
  - [ ] 步骤 5：断言进入配装区，且候选列表可正常打开。

- [x] 2.4 状态：标准模式默认激活
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：断言标准模式处于激活状态。
  - [ ] 步骤 3：断言 connection group 行支持单独选择。

- [x] 2.5 状态：标准测试状态-大太刀-炮塔标签
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：点击左侧 `slotType` 的 `turret` 标签。
  - [ ] 步骤 3：点击分组标签 `con_turret_m_01`。
  - [ ] 步骤 4：断言炮塔候选区可见，且当前标签为 `con_turret_m_01`。

- [x] 2.6 状态：标准测试状态-大太刀-简化模式
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：点击模式切换进入简化模式。
  - [ ] 步骤 3：断言 group 聚合展示可见且已配数据未变化。

- [x] 2.7 切换：标准模式->简化模式
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：在无冲突分配下点击模式切换。
  - [ ] 步骤 3：断言界面切换为 group 聚合展示，且已配数据未变化。

- [x] 2.8 切换：简化模式->标准模式
  - [ ] 步骤 1：进入“切换：标准模式->简化模式”终态。
  - [ ] 步骤 2：在简化模式完成一次批量分配后切换回标准模式。
  - [ ] 步骤 3：断言 `con_turret_m_01` 与 `con_turret_m_02` 显示同一分配结果。

- [ ] 2.9 状态：冲突置灰-同 slot.type 多装备
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：在同一 `slot.type` 下分配两种不同装备。
  - [ ] 步骤 3：断言简化模式切换控件置灰且禁用原因提示可见。

- [ ] 2.10 状态：标准测试状态-mock-同size不同tags拆分
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：在测试环境中向 `shipBuild` store 写入“基于大太刀格式的 mock patch”（具体 payload 见 `ui_knowledge.md`）。
  - [ ] 步骤 3：应用 patch 后切换到简化模式并刷新分组标签。
  - [ ] 步骤 4：断言简化模式标签已拆分为 `M1`、`M2` 两组。

## 3. Scenario Content（E2E）

- [x] 3.1 场景：Bug修复验证 BUG-001（主槽位+从属护盾不再误触发冲突）
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：切换到 `turret` 并选择 `group_back_down_mid`，在同一标签中分别为主槽位与护盾选择装备。
  - [ ] 步骤 3：观察简化模式切换按钮与提示文案，断言可正常切换（不再误置灰）。
  - [ ] **修复结果**：按钮不再错误置灰。
  - [ ] 关联缺陷：`BUG-001`（状态：`Verified`，`openspec/changes/ship-build-equipment/bugs.md`）

- [x] 3.2 场景：标准模式逐 connection group 分配
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：在 `con_turret_m_01` 选择装备后切换到 `con_turret_m_02` 再选择不同装备。
  - [ ] 步骤 3：断言两组分配可独立存在。

- [x] 3.3 场景：大太刀 advanced 配装基线
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：分别打开 `con_engine_01`、`con_shield_01`、`con_weapon_01`、`con_turret_m_01` 候选区。
  - [ ] 步骤 3：断言四类区块均存在 advanced 候选并可完成选择。

- [x] 3.4 场景：简化模式按 group 批量分配
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-简化模式”。
  - [ ] 步骤 2：对 `M1` 分组选择 `turret_gen_m_disabler_01_mk1`。
  - [ ] 步骤 3：断言该 group 下全部 connection groups 被同步配置。

- [x] 3.5 场景：同一标签内同时提供主槽位与护盾选择
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：切换到 `turret` 并进入 `group_back_down_mid` 标签，在同一标签内分别在“炮塔区”和“护盾区”进行选择。
  - [ ] 步骤 3：断言两块选择区独立生效且都归属于当前标签。

- [x] 3.6 场景：候选过滤符合 `type + size + slotTags(all)`
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：在 `con_turret_m_01` 候选列表检查候选项的 `type=turret`、`size=medium` 与 `slotTags` 全量包含关系。
  - [ ] 步骤 3：断言不满足条件的装备不出现。

- [x] 3.7 场景：`hittable/unhittable` 作为普通标签参与 ALL 匹配
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：切换到 `turret` 并进入目标分组候选列表，检查包含 `hittable/unhittable` 的样本项。
  - [ ] 步骤 3：断言匹配结果仅由 ALL 规则决定，不存在 `hittable/unhittable` 额外判定。

- [x] 3.8 场景：兼容性标签栏只显示 6 标签
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：打开候选卡片兼容性标签栏并记录标签文本。
  - [ ] 步骤 3：断言仅出现 `standard/advanced/xenon/mining/missile/highpower` 子集。

- [x] 3.9 场景：shield 不使用 `integrated` 特例
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：进入 `con_shield_01` 候选列表并观察带 `integrated` 标签的候选是否出现。
  - [ ] 步骤 3：断言未因 `integrated` 特例被额外纳入。

- [x] 3.10 场景：标准配装组件固定实现（无组件切换）
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：观察候选区头部与工具栏区域。
  - [ ] 步骤 3：断言不存在候选组件切换入口，候选区由标准配装组件固定渲染。

- [x] 3.11 场景：装备名称在标准/简化模式一致
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀”。
  - [ ] 步骤 2：标准模式选择并记录 `turret_gen_m_disabler_01_mk1` 的显示名称，然后进入“切换：标准模式->简化模式”终态查看同一装备名称。
  - [ ] 步骤 3：断言两处名称一致且为 i18n 文本。

- [x] 3.12 场景：`noplayerblueprint=true` 不参与候选
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：打开 `group_back_down_mid` 候选列表并检查 `turret_xen_m_beam_02_mk1`（`noplayerblueprint=true`）。
  - [ ] 步骤 3：断言 `turret_xen_m_beam_02_mk1` 不显示。

- [x] 3.13 场景：候选卡片不显示图片占位
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：切换到 `con_turret_m_02` 候选列表并观察候选卡片结构。
  - [ ] 步骤 3：断言仅显示文本信息，无图片占位块。

- [ ] 3.20 场景：兼容性标签白名单过滤为空时隐藏整栏
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：定位一个仅含非白名单标签的候选卡片并观察兼容性标签栏。
  - [ ] 步骤 3：断言该卡片不渲染兼容性标签栏。

- [ ] 3.21 场景：兼容性标签显示 i18n 文本
  - [ ] 步骤 1：进入“状态：标准测试状态-大太刀-炮塔标签”。
  - [ ] 步骤 2：打开候选卡片并读取兼容性标签文本。
  - [ ] 步骤 3：断言文本与 `slot_tags.json.nameId` 的当前语言翻译一致，且不出现原始 tag id。

- [x] 3.14 场景：mock-简化模式同 size 不同 tags 拆分标签
  - [ ] 步骤 1：进入“状态：标准测试状态-mock-同size不同tags拆分”。
  - [ ] 步骤 2：查看 `M1` 与 `M2` 标签对应的连接组 tags。
  - [ ] 步骤 3：断言 `M1` 与 `M2` 分别绑定不同 tags 语义，不发生错误合并。

- [x] 3.15 场景：护盾统计不跨父槽位串组
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：切换到简化模式后分别查看 `L` 与 `M` 标签护盾区计数。
  - [ ] 步骤 3：断言各自计数与父槽位实际护盾数一致（例如 `1` 与 `6`）。

- [x] 3.16 场景：大阪高炮塔数量分组稳定
  - [ ] 步骤 1：进入“状态：标准测试状态-大阪”。
  - [ ] 步骤 2：观察 `group_back_down_mid`、`group_back_mid_up`、`group_down_mid_left`、`group_down_mid_right` 的炮塔分组与计数。
  - [ ] 步骤 3：断言在高炮塔连接点下分组展示与计数保持稳定且无异常合并。

- [x] 3.17 场景：苍鹭筛选链路与配装可见性
  - [ ] 步骤 1：进入“状态：标准测试状态-苍鹭”。
  - [ ] 步骤 2：进入配装区并打开 `group_front_top_center` 候选列表。
  - [ ] 步骤 3：断言跨种族筛选链路正确，且配装区与候选列表可正常显示。

- [x] 3.18 场景：同类型多装备导致简化切换置灰
  - [ ] 步骤 1：进入“状态：冲突置灰-同 slot.type 多装备”。
  - [ ] 步骤 2：观察简化模式切换控件状态与禁用提示。
  - [ ] 步骤 3：断言控件置灰且展示禁用原因提示。

- [x] 3.19 场景：冲突解除后恢复切换
  - [ ] 步骤 1：进入”状态：冲突置灰-同 slot.type 多装备”。
  - [ ] 步骤 2：将冲突类型收敛为同一装备并观察简化模式切换控件。
  - [ ] 步骤 3：断言控件恢复可点击。

## 4 Bug 测试

- [ ] 4.1 BUG-001: 主槽位+从属护盾不误触发冲突
  - [ ] 步骤 1：进入”状态：标准测试状态-大阪”。
  - [ ] 步骤 2：切换到 `turret` 并选择 `group_back_down_mid`，在同一标签中分别为主槽位与护盾选择装备。
  - [ ] 修复前 4.1.1：观察简化模式切换按钮 #期望: [按钮置灰，显示”同类型已存在多种装备”提示]
  - [ ] 修复后 4.1.1：观察简化模式切换按钮 #期望: [按钮可点击，提示消失]
