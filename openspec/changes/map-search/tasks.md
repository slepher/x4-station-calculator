# map-search 实施任务

- [x] 1. 地图搜索状态与布局入口
- [x] 1.1 在 `MapWorkbenchView` 左上角增加搜索框容器
- [x] 1.2 候选列表改为向下弹出，不复用右侧弹出布局
- [x] 1.3 增加搜索输入、聚焦、清空与候选显示状态

- [x] 2. 搜索索引与匹配规则
- [x] 2.1 基于地图数据构造 sector 搜索索引
- [x] 2.2 支持 `name` 搜索
- [x] 2.3 支持非 `en` locale 下的 `localeName` 搜索
- [x] 2.4 实现受限的 `cluster + 完整数字` id 匹配
- [x] 2.5 禁止 `sector` / `sector + 数字` 走 id 匹配
- [x] 2.6 将 cluster id 命中展开为对应 sector 候选

- [x] 3. 地图命中高亮
- [x] 3.1 当结果数小于 `10` 时高亮全部命中 sector
- [x] 3.2 当结果数大于等于 `10` 时关闭地图批量高亮
- [x] 3.3 为单项选中态定义高优先级样式

- [x] 4. 候选点击后的聚焦行为
- [x] 4.1 暴露或复用 sector 几何中心用于聚焦
- [x] 4.2 点击候选时在 `scale < 100%` 下先抬升到 `100%`
- [x] 4.3 在当前或校正后缩放下将目标 sector 尽量居中
- [x] 4.4 点击候选后保持目标 sector 的明确选中态

- [x] 5. 清空搜索与回收逻辑
- [x] 5.1 清空搜索时清除批量高亮
- [x] 5.2 清空搜索时清除单项选中态
- [x] 5.3 清空搜索时保持当前缩放与平移不变

- [x] 6. 构建验证
- [x] 6.1 完成实现后执行 `npm run build`
- [x] 6.2 若构建失败，修复后重新构建直至通过或记录 blocker
