# Test Tasks: ship-data-import

## 1. Unit Tests（Vitest）

- 无（本变更由用户手动验证，暂无自动化测试需求；手动检查 `slots` 数组顺序、`connection.shield` 内嵌、`thruster` 输出、`production` 数组、`equipments.size` 解析，以及 `race` 提取规则）。

## 2. Web Integration / E2E

- 无（本变更由用户手动验证，暂无自动化测试需求）。
