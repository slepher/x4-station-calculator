# Test Tasks: blueprint-preset

## 1. 单元测试

- [ ] 1.1 预设普通载入：状态显示预设名，`isDirty=true`，红点不显示。
- [ ] 1.2 预设后修改任意装备：状态显示“自定义”，`isDirty=true`，红点显示。
- [ ] 1.3 载入已保存蓝图：状态显示蓝图名，`isDirty=false`，红点不显示。
- [ ] 1.4 已保存蓝图后修改装备：状态显示蓝图名，`isDirty=true`，红点显示。
- [ ] 1.5 删除当前蓝图：状态显示“自定义”，`isDirty=true`，红点显示。
- [ ] 1.6 保存蓝图：状态显示蓝图名，`isDirty=false`，红点不显示。

## 2. 关键新路径（活动蓝图 + 载入预设）

- [ ] 2.1 前置：创建并激活一个已保存蓝图（记录其 `id/name`）。
- [ ] 2.2 执行载入预设（同 ship）后断言：
  - [ ] `blueprint.id` 不变；
  - [ ] `blueprint.name` 不变；
  - [ ] `activeBlueprintId` 不变；
  - [ ] 状态显示蓝图名。
- [ ] 2.3 若预设应用带来实质变化：`isDirty=true` 且红点显示。
- [ ] 2.4 若预设应用无实质变化：`isDirty=false` 且红点不显示。
- [ ] 2.5 该状态下点击保存：应更新当前蓝图，不新增蓝图条目。

## 3. C/U 槽回归

- [ ] 3.1 修改 C 槽（导弹/消耗品）配置，断言 `isDirty` 与红点按实质变化生效。
- [ ] 3.2 修改 U 槽（无人机）配置，断言 `isDirty` 与红点按实质变化生效。
- [ ] 3.3 C/U 槽改回原值，断言回到 `isDirty=false` 且无红点。

## 4. UI 验证

- [ ] 4.1 Fit 标题栏右侧按钮可打开蓝图弹层。
- [ ] 4.2 弹层分组标题显示为 `预设配装` 与 `蓝图配装`。
- [ ] 4.3 弹层 `max-height` 为 `400px`。
