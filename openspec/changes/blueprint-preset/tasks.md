# Tasks: blueprint-preset

## 1. 入口与分组

- [ ] 1.1 将蓝图加载入口固定在 Fit 标题栏右侧。
- [ ] 1.2 在蓝图弹层中增加分组：
  - [ ] `预设配装`
  - [ ] `蓝图配装`
- [ ] 1.3 调整加载弹层最大高度为 `400px`。

## 2. 状态管理与加载语义

- [ ] 2.1 统一状态名计算规则（蓝图名/预设名/自定义）。
- [ ] 2.2 实现“活动已保存蓝图 + 载入预设”时保留当前蓝图 `id/name`。
- [ ] 2.3 保证该场景下 `saveBlueprint` 覆盖保存当前蓝图，不新建。
- [ ] 2.4 修正切船与蓝图激活态联动，避免残留 `activeBlueprintId` 影响状态显示。

## 3. Dirty 与红点

- [ ] 3.1 以实质变化驱动 `isDirty`（快照比较）。
- [ ] 3.2 比较范围覆盖连接装备与存储（含 C/U 槽）。
- [ ] 3.3 红点仅在“实质变化”时显示；预设未变化时不显示。

## 4. 回归与文案

- [ ] 4.1 校验分组文案：
  - [ ] `预设配装`
  - [ ] `蓝图配装`
- [ ] 4.2 校验“自定义”文案在无名脏状态下正确显示。
- [ ] 4.3 回归删除当前蓝图、保存后 clean、已保存蓝图修改后三态行为。
