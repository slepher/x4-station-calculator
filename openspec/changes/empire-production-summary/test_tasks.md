## Unit Tests

- [ ] analyzeEmpireWareFlow 聚合逻辑测试
  - **目标**: 验证帝国级数据聚合函数的正确性
  - **步骤**:
    1. 创建多个模拟空间站的 `GroupedFlows` 数据
    2. 调用 `analyzeEmpireWareFlow` 函数
    3. 验证补给组数据正确聚合
    4. 验证产品组数据正确归类（netRate > 0）
    5. 验证运营组数据正确归类（netRate < 0）
  - **期待结果**: 聚合结果符合预期分组逻辑

- [ ] EmpireStore 缓存机制测试
  - **目标**: 验证空间站流量缓存的初始化和更新
  - **步骤**:
    1. 初始化 EmpireStore
    2. 验证所有空间站的缓存已创建
    3. 调用 `updateStationModules` 更新某个空间站
    4. 验证该空间站的缓存已更新
  - **期待结果**: 缓存正确初始化和更新

- [ ] EmpireFlowAtom 数据结构测试
  - **目标**: 验证明细数据包含正确的空间站信息
  - **步骤**:
    1. 创建模拟的 `EmpireWareFlow` 数据
    2. 验证 `contributions` 数组包含 `stationId` 和 `stationName`
    3. 验证产出和消耗数据正确
  - **期待结果**: 明细数据结构正确

## Web Integration Tests

- [ ] 帝国总览界面渲染测试
  - **目标**: 验证帝国总览界面正确显示 EmpireWareFlowsDashboard 组件
  - **步骤**:
    1. 导航到应用
    2. 点击帝国总览 tab（activeStationId = null）
    3. 验证 EmpireWareFlowsDashboard 组件已渲染
    4. 验证三个分组（产品/运营/补给）已显示
  - **期待结果**: 帝国总览界面正确渲染

- [ ] 视图切换测试
  - **目标**: 验证数量视图和经济视图切换功能
  - **步骤**:
    1. 进入帝国总览界面
    2. 点击"数量"视图按钮
    3. 验证显示净产量数据
    4. 点击"经济"视图按钮
    5. 验证显示经济价值数据
  - **期待结果**: 视图切换正常工作

- [ ] 物品明细展开测试
  - **目标**: 验证点击物品后明细正确展开
  - **步骤**:
    1. 进入帝国总览界面
    2. 点击某个物品
    3. 验证明细面板展开
    4. 验证显示各空间站的贡献（名称和数量）
  - **期待结果**: 明细正确展开并显示空间站贡献

- [ ] 缓存同步测试
  - **目标**: 验证空间站模块更新后帝国总览数据同步更新
  - **步骤**:
    1. 进入帝国总览界面，记录某物品的净产量
    2. 切换到某个空间站
    3. 修改该空间站的模块配置
    4. 切换回帝国总览界面
    5. 验证该物品的净产量已更新
  - **期待结果**: 帝国总览数据与空间站数据同步
